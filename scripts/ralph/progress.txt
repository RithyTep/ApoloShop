# Ralph Progress Log
Started: 2025-01-12
Project: ApoloShop - Product Search Feature

## Codebase Patterns
- Components use kebab-case: `shop-header.tsx`
- UI primitives from Shadcn: `components/ui/`
- Translations in `lib/i18n.ts` with EN/KH keys
- API routes: `app/api/*/route.ts` with Prisma
- Use React Query hooks from `lib/api-hooks.ts`
- Commit format: `[feat] description` or `[fix] description`

---

## 2026-01-12 - US-001
- **Implemented**: Search API endpoint GET /api/products/search
- **Files changed**:
  - `app/api/products/search/route.ts` (new file)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - Accepts `?q=` query parameter for search term
  - Searches in nameEn, nameKh, descriptionEn, descriptionKh fields
  - Returns matching products with category info (id, nameEn, nameKh, slug)
  - Handles empty query gracefully (returns empty array)
  - Optional `?limit=` parameter (default 10)
- **Learnings for future iterations**:
  - Existing `/api/products` route has a `search` param but only searches name/sku, not description
  - Prisma `contains` with `mode: "insensitive"` works for case-insensitive search
  - Only return active products (`isActive: true`) for customer-facing search
---

## 2026-01-12 - US-002
- **Implemented**: Search input component in shop header
- **Files changed**:
  - `components/header.tsx` (modified)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - Added search input using Shadcn Input component with Search icon from lucide-react
  - Desktop: fixed width (max-w-xs) search input in header row, centered
  - Mobile: full-width search bar below header row (sm:hidden)
  - Bilingual placeholder: "Search products..." (EN) / "ស្វែងរកផលិតផល..." (KH)
  - Controlled input with optional external state management via props
- **Learnings for future iterations**:
  - Header component at `components/header.tsx` (not shop-header.tsx)
  - Uses internal state by default, but supports controlled mode via `searchQuery` and `onSearchChange` props
  - Mobile breakpoint is `sm:` (640px) - above shows inline, below shows full-width row
  - Input styling: `rounded-full` for pill shape, `pl-9` for icon padding
---

## 2026-01-12 - US-003
- **Implemented**: Search results dropdown with debounced API calls
- **Files changed**:
  - `components/search-dropdown.tsx` (new file)
  - `components/header.tsx` (modified - integrated SearchDropdown)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - 300ms debounce on search input before API call
  - Dropdown shows below search input with product results
  - Displays product image (with placeholder for missing images), name, and price
  - Bilingual "No results found" / "រកមិនឃើញលទ្ធផល" message
  - Skeleton loading state with "Searching..." / "កំពុងស្វែងរក..." text
  - Click outside to close dropdown
  - Currency-aware price display (USD/KHR)
  - Language-aware product name display (EN/KH)
- **Learnings for future iterations**:
  - SearchDropdown is separate component for reusability
  - Uses `useRef` + `mousedown` event for click-outside detection
  - Dropdown positioned with `absolute top-full` relative to search input container
  - Price formatting uses `Number()` conversion since Prisma Decimal comes as string
  - Search API returns max 5 results via `limit=5` param for dropdown brevity
---

## 2026-01-12 - US-004
- **Implemented**: Search result click navigation with keyboard support
- **Files changed**:
  - `components/search-dropdown.tsx` (modified - added navigation and keyboard)
  - `components/header.tsx` (modified - added onProductSelect handler)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - Click on search result navigates to `/shop/product/[id]`
  - Dropdown closes after navigation
  - Search input clears after navigation via `onProductSelect` callback
  - Keyboard navigation: Arrow Up/Down to highlight results
  - Enter key selects highlighted result and navigates
  - Escape key closes dropdown
  - Visual highlight on currently selected item (`bg-muted` class)
- **Learnings for future iterations**:
  - `useRouter` from `next/navigation` for App Router navigation
  - `useCallback` for memoizing handler passed to useEffect deps
  - `highlightedIndex` state tracks keyboard-selected item (-1 = none)
  - Keyboard events use `document.addEventListener` for global capture
  - `onProductSelect` optional callback lets parent know when navigation happens
---

## 2026-01-12 - US-005
- **Implemented**: Search translations for bilingual support
- **Files changed**:
  - `lib/i18n.ts` (modified - added search translations)
  - `components/header.tsx` (modified - use translations for placeholder)
  - `components/search-dropdown.tsx` (modified - use translations for noResults, searching)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - Added `search` section to both EN and KH translations
  - Translations include: placeholder, noResults, searching, viewAll
  - English: "Search products...", "No results found", "Searching...", "View all results"
  - Khmer: "ស្វែងរកផលិតផល...", "រកមិនឃើញលទ្ធផល", "កំពុងស្វែងរក...", "មើលលទ្ធផលទាំងអស់"
  - Components now import from `lib/i18n.ts` instead of hardcoding strings
- **Learnings for future iterations**:
  - Language prop is "EN" | "KH" in components but i18n keys are "en" | "kh" (lowercase)
  - Use `translations[language === "EN" ? "en" : "kh"]` for mapping
  - Keep translation structure consistent with existing sections (orderStatus, receipt, etc.)
---

## 2026-01-12 - US-006
- **Implemented**: Search API security features
- **Files changed**:
  - `app/api/products/search/route.ts` (modified - added security features)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - Input validation: max query length 100 characters (returns 400 if exceeded)
  - XSS sanitization: strips HTML tags and dangerous characters (<>"'`)
  - Suspicious pattern detection: blocks <script>, javascript:, event handlers, etc.
  - In-memory rate limiting: 60 requests per minute per IP
  - Rate limit headers: X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset
  - Returns 400 for invalid/malformed queries with descriptive error messages
  - Logs suspicious search patterns with client IP for monitoring
  - Validates limit parameter (positive integer, max 50)
- **Learnings for future iterations**:
  - In-memory rate limiter works for single server but won't persist across restarts or scale horizontally
  - For production, consider Redis-based rate limiting for distributed deployments
  - Client IP extracted from x-forwarded-for (proxy) or x-real-ip headers
  - 429 status code is standard for rate limit exceeded responses
  - Sanitization happens AFTER suspicious pattern check to preserve evidence for logging
---

## 2026-01-12 - US-007
- **Implemented**: Whitelabel product filtering for search API
- **Files changed**:
  - `app/api/products/search/route.ts` (modified - added inStock filter and inventory include)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - `isActive: true` filter already present (active products only)
  - Added `?inStock=true` query param to filter out-of-stock products
  - Filters by `inventory.quantity > 0` when inStock=true
  - Includes inventory data in response for display purposes
  - Single-tenant architecture: no shopId filtering needed (schema has no multi-tenant fields)
  - `isVisible` field doesn't exist in Product schema, so not applicable
- **Learnings for future iterations**:
  - Prisma relation filtering: `whereClause.inventory = { quantity: { gt: 0 } }`
  - The schema is single-tenant (no shopId/tenantId on Product model)
  - Inventory is optional relation - products without inventory won't match `inStock=true` filter
  - Include inventory in response so frontend can show stock status
  - Use TypeScript's `Parameters<typeof prisma.product.findMany>[0]["where"]` for type-safe where clause building
---

## 2026-01-12 - US-008
- **Implemented**: Search result branding support for whitelabel customization
- **Files changed**:
  - `components/search-dropdown.tsx` (modified - added SearchBranding interface and branding props)
  - `components/header.tsx` (modified - added searchBranding prop and passed to SearchDropdown)
  - `components/shop-app.tsx` (modified - passes theme branding to Header)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Primary color for search highlight**: Keyboard-highlighted items use shop's primaryColor with white text
  - **Custom no results message**: Supports customNoResultsMessage.en/kh override from branding settings
  - **Font settings**: Dropdown inherits parent font via CSS cascade (uses --foreground CSS var)
  - **Currency display**: Already respected via currency prop (USD/KHR)
  - **Border radius**: Dropdown corners respect shop's borderRadius theme setting
- **Learnings for future iterations**:
  - ShopTheme interface in lib/api-hooks.ts has: primaryColor, accentColor, backgroundColor, textColor, borderRadius
  - Theme doesn't have explicit fontFamily field - fonts cascade from parent container CSS vars
  - Create separate SearchBranding interface to avoid coupling to full ShopTheme
  - Use inline styles for conditional branding (when primaryColor exists) vs Tailwind classes (fallback)
  - shop-app.tsx applies theme via CSS variables: --primary, --accent, --background, --foreground, --radius
---

## 2026-01-12 - US-009
- **Implemented**: Search analytics tracking
- **Files changed**:
  - `prisma/schema.prisma` (modified - added SearchLog model)
  - `app/api/products/search/route.ts` (modified - added query logging)
  - `app/api/analytics/search/route.ts` (new file - analytics endpoint)
  - `lib/api-hooks.ts` (modified - added useSearchAnalytics hook and types)
  - `components/popular-searches-widget.tsx` (new file - dashboard widget)
  - `components/pages/dashboard-page.tsx` (modified - integrated PopularSearchesWidget)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **SearchLog model**: Tracks query, resultsCount, ipAddress, userAgent, createdAt
  - **Non-blocking logging**: Search API logs queries without affecting response time
  - **Analytics endpoint**: GET /api/analytics/search with date range filtering
  - **Summary stats**: totalSearches, successRate, zeroResultSearches
  - **Popular searches**: Grouped by query with count and avgResults
  - **Zero-result queries**: Highlights searches returning no results for product opportunities
  - **Dashboard widget**: Shows summary, popular searches, and zero-result warnings
- **Learnings for future iterations**:
  - Non-blocking logging pattern: `prisma.create().catch()` for fire-and-forget DB writes
  - Prisma groupBy with aggregations: `_count`, `_avg` for analytics queries
  - Raw SQL for complex date groupings: `prisma.$queryRaw` with DATE() function
  - Admin widgets should be self-contained with their own data fetching via hooks
  - Layout pattern: Use grid with `lg:col-span-1` and `lg:col-span-2` for asymmetric layouts
---

## 2026-01-12 - US-010
- **Implemented**: Dynamic component registry system
- **Files changed**:
  - `lib/component-registry.ts` (new file - registry module)
  - `lib/api-hooks.ts` (modified - added useComponentRegistry and useUpdateComponentRegistry hooks)
  - `components/pages/settings-page.tsx` (modified - added Component Settings section)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **ComponentRegistration type**: id, name, component, defaultEnabled, category, description
  - **Register/get functions**: registerComponent, getComponent, getAllComponents, getComponentsByCategory
  - **Lazy loading support**: registerLazyComponent helper using React.lazy()
  - **Config helpers**: getDefaultConfig, getEnabledComponents, mergeWithDefaults for state management
  - **Admin toggle UI**: Component Settings card in Settings page with enable/disable switches
  - **Persistence**: Uses existing settings API to store componentRegistry config
- **Learnings for future iterations**:
  - Registry uses Map internally for O(1) lookups by component ID
  - ComponentType<P> | LazyExoticComponent<ComponentType<P>> allows both eager and lazy components
  - mergeWithDefaults handles new components being added after config is saved
  - Admin UI shows empty state message when no components are registered
  - Settings page pattern: separate mutation hook (useUpdateComponentRegistry) for specific settings
---

## 2026-01-12 - US-011
- **Implemented**: Dynamic page builder blocks (Testimonials, Features, FAQ)
- **Files changed**:
  - `lib/api-hooks.ts` (modified - added TestimonialsConfig, FeaturesConfig, FAQConfig types and updated SectionType union)
  - `components/customizer/editors/testimonials-editor.tsx` (new file)
  - `components/customizer/editors/features-editor.tsx` (new file)
  - `components/customizer/editors/faq-editor.tsx` (new file)
  - `components/customizer/renderers/testimonials-renderer.tsx` (new file)
  - `components/customizer/renderers/features-renderer.tsx` (new file)
  - `components/customizer/renderers/faq-renderer.tsx` (new file)
  - `components/customizer/customizer-page.tsx` (modified - integrated new block types)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Testimonials block**: Customer reviews with 5-star ratings, name, role, photo, and testimonial text
  - **Features block**: Icon-based features with 15 lucide icons to choose from, title, and description
  - **FAQ block**: Accordion-style FAQ with expandable question/answer pairs
  - All blocks support bilingual content (EN/KH)
  - All blocks have configurable layout options (grid/list, columns)
  - Drag-and-drop reordering already supported via existing @dnd-kit implementation
  - Preview mode shows live changes immediately
- **Learnings for future iterations**:
  - Existing Hero, ProductGrid (products), and Banner (promotions) blocks already exist
  - Pattern: Each block type needs an editor component and a renderer component
  - sectionIcons, sectionLabels, and defaultSectionConfigs records must all be updated together
  - Accordion for FAQ uses simple state with Set<string> for tracking open items
  - Feature icons are stored as string identifiers and mapped to components at render time
---

## 2026-01-12 - US-012
- **Implemented**: Admin sales report dashboard
- **Files changed**:
  - `app/api/reports/sales/route.ts` (new file)
  - `lib/api-hooks.ts` (modified - added SalesReport types and useSalesReport hook)
  - `components/pages/reports-page.tsx` (modified - complete rewrite to use dedicated sales API)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **GET /api/reports/sales endpoint**: Accepts startDate/endDate query params, defaults to last 30 days
  - **Summary metrics**: totalRevenue, totalRevenueKhr, orderCount, cancelledOrders, averageOrderValue
  - **Comparison metrics**: revenueChange and orderCountChange vs previous period
  - **topProducts**: Top 10 products by revenue with rank, quantity, category info, imageUrl
  - **salesByCategory**: Revenue and percentage breakdown by product category
  - **salesByChannel**: Revenue and percentage breakdown by order channel (WEBSITE, TELEGRAM, etc.)
  - **topCustomers**: Top 10 customers by revenue with order count and AOV
  - **chartData**: Daily sales data for time-series charts (date, revenue, orderCount, averageOrderValue)
  - **Reports page visualizations**: AreaChart (revenue trend), BarChart (orders by day), PieCharts (category/channel distribution), LineChart (AOV trend), Tables (top products/customers)
  - **CSV export**: Uses existing /api/export endpoint with type=sales
- **Learnings for future iterations**:
  - Prisma Decimal types need Number() conversion for arithmetic operations
  - Use Map<string, T> for efficient grouping and aggregation in memory
  - Previous period comparison calculated using same duration window before startDate
  - Charts use Recharts library (already installed) - AreaChart good for trends, BarChart for counts, PieChart for distributions
  - reports-page.tsx already existed but used dashboard API; rewrote to use dedicated sales API for proper separation of concerns
---

## 2026-01-12 - US-013
- **Implemented**: Admin customer analytics report
- **Files changed**:
  - `app/api/reports/customers/route.ts` (new file)
  - `lib/api-hooks.ts` (modified - added CustomerReport types and useCustomerReport hook)
  - `components/pages/reports-page.tsx` (modified - added Customers tab with full analytics UI)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **GET /api/reports/customers endpoint**: Accepts startDate/endDate query params, defaults to last 30 days
  - **Summary metrics**: totalCustomers, newCustomers, newCustomersChange, returningCustomers, activeCustomers, retentionRate, avgCustomerLifetimeValue
  - **Top customers by LTV**: Ranked list with totalSpent, orderCount, avgOrderValue, lastOrderDate
  - **Customer growth over time**: Monthly data with newCustomers and totalCustomers cumulative count
  - **Customer segments**: VIP (5+ orders, $100+ spent), Active (ordered in last 30 days), At Risk (30-90 days since last order), Churned (90+ days), New (created in last 30 days, no orders)
  - **Tabbed reports UI**: Added Sales and Customers tabs with TabsList from Shadcn
  - **Retention rate visualization**: Circular progress ring showing percentage of returning customers
  - **Segments pie chart**: Color-coded with icons for each segment type
- **Learnings for future iterations**:
  - Customer segmentation logic uses recency (lastOrderDate) and frequency (orderCount) metrics
  - Retention rate = (customers who ordered in both previous and current period) / (customers who ordered in previous period)
  - Customer growth chart uses monthly aggregation via YYYY-MM date keys
  - Tab-based UI pattern: useQuery hooks fetch both datasets immediately, renders based on active tab
  - LTV (Lifetime Value) calculated as sum of all completed order totals per customer
  - Segment thresholds are currently hardcoded - could be made configurable in future
---

## 2026-01-12 - US-014
- **Implemented**: Admin inventory report
- **Files changed**:
  - `app/api/reports/inventory/route.ts` (new file)
  - `lib/api-hooks.ts` (modified - added InventoryReport types and useInventoryReport hook)
  - `components/pages/reports-page.tsx` (modified - added Inventory tab with full visualizations)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **GET /api/reports/inventory endpoint**: Returns comprehensive inventory status report
  - **Summary metrics**: totalProducts, productsWithInventory, lowStockCount, criticalStockCount, outOfStockCount, healthyStockCount, totalInventoryValue (USD/KHR)
  - **Low stock products**: With critical/low status indicators, sorted by severity
  - **Out of stock products**: Products with zero quantity or no inventory record
  - **Stock by category**: Breakdown of inventory value and stock alerts per category
  - **Fast-moving products**: High sales velocity items with daysUntilStockout calculation
  - **Slow-moving products**: No sales in 30 days, tied-up inventory value
  - **Configurable threshold**: `?threshold=` query param for custom low stock level
  - **Inventory tab**: Added to reports page with summary cards, pie chart, bar chart, and multiple tables
- **Learnings for future iterations**:
  - Critical status = quantity <= 50% of minLevel; low status = quantity <= minLevel
  - Products without inventory records are treated as out-of-stock
  - Fast-moving = sold 10+ units in last 30 days; daysUntilStockout = currentStock / dailySaleRate
  - Slow-moving = has stock but zero sales in 30 days
  - Email/notification alerts were marked optional in acceptance criteria - could be added later via webhook integration
  - Inventory report doesn't use date range since it's a snapshot of current state
---

## 2026-01-12 - US-015
- **Implemented**: Multi-tenant client model
- **Files changed**:
  - `prisma/schema.prisma` (modified - added Client model, clientId to Product/Order/Customer/ShopCustomization)
  - `app/api/clients/route.ts` (new file - CRUD endpoint with super admin auth)
  - `lib/client-middleware.ts` (new file - client detection utilities)
  - `middleware.ts` (new file - Next.js middleware for request interception)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Client model**: id, name, slug (unique, for subdomains), domain (unique, for custom domains), settings (JSON), isActive, timestamps
  - **Relations**: Client has many Products, Orders, Customers, ShopCustomizations (optional clientId on each)
  - **GET /api/clients**: List clients with pagination, search, isActive filter, includes _count for products/orders/customers
  - **POST /api/clients**: Create client with slug/domain uniqueness validation
  - **PUT /api/clients**: Update client with uniqueness checks
  - **DELETE /api/clients**: Soft delete (deactivate) if has data, force delete with cascade option
  - **Super admin auth**: Uses x-admin-key header or SUPER_ADMIN_KEY env var
  - **Client middleware**: extractClientIdentifier (domain vs subdomain vs default), findClientByIdentifier, getClientFromRequest, withClient/requireClient HOCs
  - **Next.js middleware**: Extracts client slug from hostname, injects x-client-slug header for API routes
- **Learnings for future iterations**:
  - Used optional clientId (nullable) for backwards compatibility with existing single-tenant data
  - Slug is for subdomains ({slug}.apoloshop.com), domain is for custom domains (shop.example.com)
  - Middleware is lightweight (edge runtime) - database lookup happens in API route via client-middleware.ts
  - BASE_DOMAIN env var controls subdomain extraction (defaults to apoloshop.com)
  - DEFAULT_CLIENT_SLUG env var for fallback when no client detected (defaults to "default")
  - Custom domains are prefixed with "domain:" in middleware header to distinguish from slugs
  - Build passes but database migration needed to add client_id columns to actual DB
---

## 2026-01-12 - US-017
- **Implemented**: Client domain routing for multi-tenant SaaS
- **Files changed**:
  - `lib/client-middleware.ts` (modified - added parseClientSlugHeader, fallback logic, helper functions)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **parseClientSlugHeader()**: Parses middleware's x-client-slug header, handles "domain:" prefix for custom domains
  - **Enhanced getClientFromRequest()**: Now uses middleware-injected header first, with automatic fallback
  - **Fallback to default client**: When domain/subdomain not found, automatically falls back to DEFAULT_CLIENT_SLUG
  - **getClientSlugFromHeaders()**: Helper for server components to parse client slug from headers
  - **createDefaultClientContext()**: Creates a synthetic default client context when no DB record exists
  - **getClientFromRequestWithFallback()**: Guaranteed non-null client context retrieval
  - **Warning logs**: Logs when falling back to default for monitoring/debugging
- **Learnings for future iterations**:
  - Middleware (edge runtime) can't do DB lookups - passes slug via header to API routes
  - "domain:" prefix distinguishes custom domains from subdomain slugs
  - Fallback pattern ensures app works even if client record missing from DB
  - Three-tier resolution: x-client-id header -> x-client-slug header -> hostname extraction
  - createDefaultClientContext() useful for tests and development without DB
  - Build still passes even though DB migration for client_id columns pending
---

## 2026-01-12 - US-018
- **Implemented**: Recently viewed products tracking and display
- **Files changed**:
  - `lib/use-recently-viewed.ts` (new file - localStorage hook)
  - `components/recently-viewed.tsx` (new file - display component)
  - `app/shop/product/[id]/page.tsx` (modified - integrated hook and component)
  - `lib/i18n.ts` (modified - added recentlyViewed translations)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **useRecentlyViewed hook**: Manages localStorage with max 10 product IDs, most recent first
  - **addViewedProduct()**: Adds product to list, removes duplicates, persists to localStorage
  - **clearRecentlyViewed()**: Clears all history with localStorage.removeItem
  - **getViewedExcluding()**: Get IDs excluding current product (for product page display)
  - **RecentlyViewed component**: Grid display with product cards, image, name, price
  - **Loading state**: Skeleton cards while fetching product details from API
  - **Clear button**: UI option to clear viewing history
  - **Bilingual support**: "Recently Viewed" / "បានមើលថ្មីៗ" translations
  - **Responsive grid**: 2 cols on mobile, 4 cols on desktop
  - **Product detail integration**: Auto-tracks viewed product on page load
- **Learnings for future iterations**:
  - localStorage hook pattern: useEffect for initial load, useCallback for updates
  - Product fetching: parallel Promise.all for multiple product IDs
  - useEffect dependency for array: use .join(",") to create stable dependency string
  - Component hides itself when no products to show (returns null)
  - excludeProductId prop prevents showing current product in its own recently viewed section
---

## 2026-01-12 - US-019
- **Implemented**: Product recommendations
- **Files changed**:
  - `app/api/products/[id]/recommendations/route.ts` (new file)
  - `components/product-recommendations.tsx` (new file)
  - `lib/api-hooks.ts` (modified - added types and useProductRecommendations hook)
  - `lib/i18n.ts` (modified - added recommendations translations)
  - `app/shop/product/[id]/page.tsx` (modified - integrated ProductRecommendations)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **GET /api/products/[id]/recommendations endpoint**: Returns up to 20 products (configurable via ?limit=)
  - **"Customers also bought" logic**: Analyzes order history to find products frequently purchased together
  - **Same category fallback**: Fills remaining slots with products from same category
  - **Recommendation types**: Each product tagged as "also_bought" or "same_category"
  - **Response meta**: Includes total count, alsoBoughtCount, sameCategoryCount
  - **ProductRecommendations component**: Grid display with skeleton loading, handles empty/error states gracefully
  - **"Popular" badge**: Shows on "also_bought" products to indicate customer preference
  - **Out of stock indicator**: Shows badge overlay when product inventory is zero
  - **Bilingual support**: "You May Also Like" / "អ្នកក៏អាចចូលចិត្ត" translations
- **Learnings for future iterations**:
  - Co-purchase frequency analysis: Count occurrences in same orders, sort by frequency
  - Prisma distinct: Use for getting unique order IDs containing a product
  - Order status filter: Only completed/confirmed orders for reliable co-purchase data
  - Combine strategies: "also_bought" first (higher conversion), then "same_category" to fill slots
  - Return recommendationType so frontend can style differently (Popular badge)
  - Product includes inventory for stock status display
---

## 2026-01-12 - US-020
- **Implemented**: Wishlist functionality
- **Files changed**:
  - `prisma/schema.prisma` (modified - added Wishlist model with relations)
  - `app/api/wishlist/route.ts` (new file - GET/POST/DELETE endpoint)
  - `lib/use-wishlist.ts` (new file - localStorage + API sync hook)
  - `components/wishlist-button.tsx` (new file - heart icon button)
  - `components/product-grid.tsx` (modified - added WishlistButton)
  - `components/shop/products-section.tsx` (modified - added WishlistButton)
  - `app/shop/wishlist/page.tsx` (new file - wishlist page)
  - `components/wishlist-content.tsx` (new file - wishlist page content)
  - `lib/i18n.ts` (modified - added wishlist translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Wishlist model**: id, customerId (optional), productId, guestId (optional), createdAt
  - **Dual unique constraints**: customerId+productId and guestId+productId for both user types
  - **GET /api/wishlist**: Fetch wishlist items with product details (category, inventory)
  - **POST /api/wishlist**: Add product to wishlist with duplicate check
  - **DELETE /api/wishlist**: Remove by wishlist ID or productId+guestId/customerId
  - **useWishlist hook**: Optimistic updates with localStorage, non-blocking API sync
  - **WishlistButton**: Icon variant (on product card hover) and button variant
  - **Heart animation**: Fill animation when product is wishlisted
  - **Wishlist page**: Grid display with product cards, add-to-cart, remove buttons
  - **Bilingual support**: EN/KH translations for all wishlist UI text
- **Learnings for future iterations**:
  - Guest ID generation: Use timestamp + random string for uniqueness
  - Optimistic updates: Update local state immediately, sync API in background
  - localStorage for guests: Persist IDs locally, fetch full data from API when needed
  - Group hover pattern: Use `group-hover:opacity-100` for show-on-hover UI elements
  - Wishlist API returns filtered products (only isActive=true)
  - guestId stored in separate localStorage key for consistent identification
---

## 2026-01-12 - US-021
- **Implemented**: Product quick view modal
- **Files changed**:
  - `components/product-quick-view.tsx` (new file - modal and button components)
  - `components/product-grid.tsx` (modified - integrated quick view)
  - `components/shop/products-section.tsx` (modified - integrated quick view)
  - `lib/i18n.ts` (modified - added quickView translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **ProductQuickView modal**: Shows product image, name, price, description, add to cart button
  - **Image gallery**: Main image with thumbnail strip, left/right navigation arrows
  - **QuickViewButton component**: Eye icon button that appears on product card hover
  - **Quantity selector**: Respects stock limits, increment/decrement buttons
  - **Stock status display**: Shows "In Stock" badge with quantity available
  - **Out of stock overlay**: Dimmed overlay with badge when product is unavailable
  - **Category badge**: Shows product category in modal
  - **Bilingual support**: All UI text in English and Khmer
  - **Wishlist integration**: Heart button in modal header
- **Learnings for future iterations**:
  - Product type already has `images?: string[]` array for multiple product images
  - Existing Dialog component from Shadcn works well as base for modals
  - Use `pointer-events-none` on overlay container and `pointer-events-auto` on button for click-through behavior
  - Image gallery pattern: combine imageUrl (primary) with images array (additional)
  - Replaced old inline Dialog in product-grid.tsx with reusable ProductQuickView component
  - QuickViewButton uses `e.preventDefault()` and `e.stopPropagation()` to prevent card click propagation
---

## 2026-01-12 - US-022
- **Implemented**: Advanced product filtering
- **Files changed**:
  - `components/filter-sidebar.tsx` (new file - FilterSidebar and MobileFilterTrigger components)
  - `components/ui/slider.tsx` (new file - Shadcn Slider component)
  - `components/ui/checkbox.tsx` (new file - Shadcn Checkbox component)
  - `components/ui/collapsible.tsx` (new file - Shadcn Collapsible component)
  - `app/shop/products/page.tsx` (new file - Products page with filters)
  - `components/shop/products-page-client.tsx` (new file - Client component for products page)
  - `lib/i18n.ts` (modified - added filters translations EN/KH)
  - `lib/server-utils.ts` (modified - added getCategories function)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **FilterSidebar component**: Collapsible sections using Shadcn Collapsible
  - **Price range slider**: Dual-handle slider with min/max input fields, currency display
  - **Category checkbox filter**: Multi-select categories via Shadcn Checkbox
  - **Sort options**: 6 options (newest, price_asc, price_desc, popular, name_asc, name_desc)
  - **In-stock filter**: Toggle to show only products with inventory
  - **URL query params sync**: Filters persist to URL for shareable links
  - **Mobile support**: MobileFilterTrigger button opens filter dialog on small screens
  - **Bilingual support**: All UI text in English and Khmer
  - **Products page**: /shop/products with sidebar layout (desktop) and dialog (mobile)
- **Learnings for future iterations**:
  - Shadcn Slider supports dual handles via array value: `value={[min, max]}`
  - useSearchParams() hook for reading URL params on mount
  - router.push() with scroll: false to update URL without scrolling
  - Collapsible component needs CollapsibleTrigger and CollapsibleContent children
  - ShopClientWrapper provides cart/language/currency context, not a separate ShopProvider
  - getCategories added to server-utils.ts for server-side category fetching
  - Filter state synced to URL on every change for shareable filtered views
---

## 2026-01-12 - US-023
- **Implemented**: Product reviews and ratings
- **Files changed**:
  - `prisma/schema.prisma` (modified - added Review model and ReviewStatus enum)
  - `app/api/products/[id]/reviews/route.ts` (new file - GET/POST endpoint)
  - `app/api/reviews/route.ts` (new file - admin GET all reviews endpoint)
  - `app/api/reviews/[id]/route.ts` (new file - admin PATCH/DELETE endpoint)
  - `components/star-rating.tsx` (new file - StarRating and InteractiveStarRating components)
  - `components/product-reviews.tsx` (new file - ProductReviews component)
  - `components/pages/reviews-page.tsx` (new file - admin reviews moderation page)
  - `components/ui/progress.tsx` (new file - Shadcn Progress component)
  - `components/product-grid.tsx` (modified - added rating display on cards)
  - `components/sidebar.tsx` (modified - added reviews nav item)
  - `components/admin-dashboard.tsx` (modified - integrated ReviewsPage)
  - `lib/api-hooks.ts` (modified - added review types and hooks)
  - `lib/i18n.ts` (modified - added reviews translations EN/KH)
  - `app/shop/product/[id]/page.tsx` (modified - integrated ProductReviews and rating)
  - `package.json` (modified - added @radix-ui/react-progress)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Review model**: id, productId, customerId, guestName, rating (1-5), comment, status (PENDING/APPROVED/REJECTED), timestamps
  - **GET /api/products/[id]/reviews**: Fetch reviews with stats (average, distribution), pagination, optional includeUnapproved for admin
  - **POST /api/products/[id]/reviews**: Submit review with rating (required), comment (optional), customerId or guestName, XSS sanitization
  - **GET /api/reviews**: Admin endpoint to fetch all reviews with status filter
  - **PATCH /api/reviews/[id]**: Admin endpoint to approve/reject reviews
  - **DELETE /api/reviews/[id]**: Admin endpoint to delete reviews
  - **StarRating component**: Renders 1-5 stars with partial fill support, multiple sizes (sm/md/lg)
  - **InteractiveStarRating component**: Clickable star rating for review form
  - **ProductReviews component**: Full review section with stats, rating distribution bars, review list, submit form, pagination
  - **ReviewsPage**: Admin moderation dashboard with status filter, stats cards, approve/reject/delete actions, delete confirmation
  - **Rating on product cards**: useProductRatings hook fetches ratings for visible products
  - **Sidebar integration**: Reviews nav item with pending count badge
  - **Bilingual support**: All UI text in English and Khmer
- **Learnings for future iterations**:
  - Review moderation pattern: default status PENDING, admin approves/rejects before public display
  - Rating distribution: Prisma groupBy with _count aggregation for star counts
  - XSS prevention: Sanitize user input before storing (comment, guestName)
  - Duplicate review prevention: Check existing review from same customer before allowing new one
  - Progress component from Radix UI for rating distribution bars
  - useProductRatings hook batches rating requests for multiple products
  - Admin badge counts: Fetch pending count separately for sidebar notification
---

## 2026-01-12 - US-024
- **Implemented**: Order status notifications
- **Files changed**:
  - `prisma/schema.prisma` (modified - added NotificationPreference, NotificationLog models, NotificationChannel enum)
  - `lib/notification-service.ts` (new file - notification service with Telegram/Email support)
  - `app/api/notifications/preferences/route.ts` (new file - GET/POST/PUT/DELETE for preferences)
  - `app/api/orders/route.ts` (modified - auto-send notifications on status change)
  - `components/notification-preferences.tsx` (new file - UI component for managing preferences)
  - `components/pages/customers-page.tsx` (modified - added Notifications tab in customer details dialog)
  - `lib/i18n.ts` (modified - added notifications translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **NotificationPreference model**: customerId, channel (TELEGRAM/EMAIL/SMS), enabled, telegramChatId, per-status toggles
  - **NotificationLog model**: Tracks all notification attempts with status/errors
  - **Telegram support**: Uses TELEGRAM_BOT_TOKEN env var, sends via Telegram Bot API
  - **Email support**: Optional SMTP configuration (SMTP_HOST/PORT/USER/PASS/FROM), uses nodemailer
  - **Order status triggers**: Auto-notification on status change (CONFIRMED/PREPARING/READY/COMPLETED/CANCELLED)
  - **Preferences API**: Full CRUD at /api/notifications/preferences
  - **Customer UI**: Tab-based dialog showing Details + Notifications, per-channel settings with status toggles
  - **Bilingual support**: EN/KH translations for all notification UI text
- **Learnings for future iterations**:
  - Fire-and-forget pattern: Call notification service without awaiting to avoid blocking API response
  - nodemailer is optional dependency - dynamic import with catch fallback
  - Telegram Bot API requires chat_id from user - users must start conversation with bot first
  - Per-status preferences allow granular control (e.g., only notify on "Ready" status)
  - Prisma unique constraint on [customerId, channel] ensures one preference per customer per channel
  - Tabs component from Shadcn works well for organizing related content in dialogs
---

## 2026-01-12 - US-025
- **Implemented**: Admin dashboard widgets with drag-and-drop
- **Files changed**:
  - `components/dashboard-widgets.tsx` (new file - main widgets component)
  - `components/pages/dashboard-page.tsx` (modified - simplified to use DashboardWidgets)
  - `lib/api-hooks.ts` (modified - added widget types and layout hooks)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **DashboardWidgetType**: sales_overview, recent_orders, low_stock, top_products
  - **DashboardWidgetConfig**: id, type, order, enabled, size properties
  - **SalesOverviewWidget**: KPIs showing today orders, revenue, pending orders, low stock items with trends
  - **RecentOrdersWidget**: Table of latest 5 orders with status badges
  - **LowStockWidget**: List of products below min inventory with critical/warning status
  - **TopProductsWidget**: Bar chart of orders by day using Recharts
  - **Drag-and-drop**: Using @dnd-kit/core and @dnd-kit/sortable with rectSortingStrategy
  - **Layout persistence**: Saves to settings API with useDashboardLayout/useUpdateDashboardLayout hooks
  - **Auto-refresh**: 5-minute interval via useEffect + setInterval, with manual refresh button
  - **Customize dialog**: Toggle widget visibility, shows all widgets with descriptions
  - **SortableWidget wrapper**: Combines drag handle, visibility switch, and content rendering
- **Learnings for future iterations**:
  - rectSortingStrategy works better for grid layouts than verticalListSortingStrategy
  - Layout persistence via settings API is simple and works well for per-user preferences
  - Widget configs use defaultDashboardWidgets as fallback when no saved layout exists
  - useLowStockItems hook already existed and was reused for LowStockWidget
  - Auto-refresh pattern: useEffect with setInterval cleanup, plus manual refetch function
  - Switch component for visibility toggle provides immediate feedback
  - DndContext + SortableContext pattern is consistent with existing customizer implementation
---

## 2026-01-12 - US-026
- **Implemented**: Bulk product import/export
- **Files changed**:
  - `app/api/products/export/route.ts` (new file - GET endpoint for CSV export)
  - `app/api/products/import/route.ts` (new file - POST endpoint for CSV import)
  - `components/product-import-export.tsx` (new file - ProductImportDialog and ProductExportButton)
  - `components/ui/alert.tsx` (new file - Shadcn Alert component)
  - `components/ui/scroll-area.tsx` (new file - Shadcn ScrollArea component)
  - `components/pages/products-page.tsx` (modified - integrated import/export buttons)
  - `lib/api-hooks.ts` (modified - added ImportResult types, useImportProducts hook, exportProducts function)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **GET /api/products/export**: Returns CSV with all product fields including inventory, filterable by category/status
  - **POST /api/products/import**: Accepts multipart form CSV upload with FormData
  - **CSV fields**: sku, nameEn, nameKh, descriptionEn, descriptionKh, priceUsd, priceKhr, categorySlug, stock, minStockLevel, imageUrl, images, isActive
  - **Validation**: Zod schema validation per row, category lookup validation, SKU uniqueness check
  - **Error reporting**: Row-by-row errors with field name, message, and original row data
  - **Dry run mode**: dryRun=true validates without making changes, returns pendingCreates/pendingUpdates counts
  - **Update existing**: updateExisting=true allows updating products by SKU match
  - **ProductImportDialog**: Four stages (select/validating/importing/complete), progress bar, file drag-drop, result summary with error scroll
  - **ProductExportButton**: Download CSV with current filters applied
- **Learnings for future iterations**:
  - CSV parsing: Handle quoted fields with escaped quotes (RFC 4180 format)
  - FormData upload: Don't set Content-Type header manually, let browser handle multipart boundary
  - Dry run pattern: Validate and return counts before actual import for user confirmation
  - XSS sanitization: Apply to text fields during import to prevent stored XSS
  - Import result types: Use ImportRowError with row number for precise error location
  - Progress stages: Simple state machine (select -> validating -> complete | select -> validating -> importing -> complete)
  - Blob download: Use URL.createObjectURL() + click() + revokeObjectURL() pattern
---

## 2026-01-12 - US-027
- **Implemented**: Super admin client management
- **Files changed**:
  - `app/api/clients/[id]/stats/route.ts` (new file - client stats endpoint)
  - `components/pages/super-admin-page.tsx` (new file - main super admin UI)
  - `lib/api-hooks.ts` (modified - added Client types and hooks)
  - `components/admin-dashboard.tsx` (modified - integrated SuperAdminPage)
  - `components/sidebar.tsx` (modified - added super-admin nav item)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **SuperAdminPage component**: Full client management dashboard in Developer section
  - **Client list with search**: Searchable table with pagination (10 per page)
  - **Status filter**: Filter by all/active/inactive clients
  - **Summary cards**: Total clients, active clients, total products, total orders
  - **Create dialog**: Create new client with name, slug, domain, currency, language, isActive
  - **Edit dialog**: Update client settings with validation
  - **View dialog**: Detailed client info with statistics (revenue, orders, AOV, products, customers)
  - **Delete confirmation**: AlertDialog with soft-delete warning for clients with data
  - **Client stats API**: GET /api/clients/[id]/stats returns client with revenue/order aggregates
  - **Impersonate feature**: setClientImpersonation/clearClientImpersonation helpers using localStorage
  - **useClients hook**: Fetch clients with pagination, search, status filter
  - **useClientStats hook**: Fetch single client with detailed stats
  - **useCreateClient/useUpdateClient/useDeleteClient hooks**: CRUD mutations with cache invalidation
- **Learnings for future iterations**:
  - Client types: Client, ClientStats, ClientWithStats, ClientsResponse for type safety
  - Impersonation pattern: Store clientId/clientSlug in localStorage for cross-tab persistence
  - Stats aggregation: Use Prisma aggregate for _sum and _count on orders table
  - Soft delete: Clients with products/orders/customers get isActive=false instead of deletion
  - Developer section: Super admin only visible in development mode (via process.env.NODE_ENV)
  - Dialog state pattern: Separate state for create/edit/view/delete dialogs for clean UX
---

## 2026-01-12 - US-028
- **Implemented**: Client subscription/billing model
- **Files changed**:
  - `prisma/schema.prisma` (modified - added Subscription model, SubscriptionPlan/SubscriptionStatus enums)
  - `lib/subscription.ts` (new file - plan limits, pricing, usage helpers)
  - `app/api/subscription/route.ts` (new file - GET/POST/DELETE endpoint)
  - `lib/api-hooks.ts` (modified - added subscription types and hooks)
  - `components/subscription-usage-card.tsx` (new file - SubscriptionUsageCard and UpgradePrompt)
  - `lib/i18n.ts` (modified - added subscription translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Subscription model**: clientId, plan (FREE/STARTER/PRO), status (ACTIVE/CANCELLED/EXPIRED/PAST_DUE), currentPeriodStart/End, usage tracking
  - **Plan limits configuration**: 
    - FREE: 20 products, 50 orders/month, 100 customers
    - STARTER: 100 products, 500 orders/month, 1000 customers, custom domain, analytics, branding, exports
    - PRO: Unlimited everything, all features including API access, priority support, multiple users
  - **Plan pricing**: $0/$19/$49 monthly, yearly discounts (17-20%)
  - **GET /api/subscription**: Returns subscription with usage stats, limits, upgrade recommendations
  - **POST /api/subscription**: Create/upgrade subscription with plan and billing cycle
  - **DELETE /api/subscription**: Cancel subscription (soft cancel at period end)
  - **Usage calculation**: calculateUsageStatus with approaching limit (80%) and at limit detection
  - **Upgrade recommendations**: getUpgradeRecommendation returns shouldUpgrade, reason, suggestedPlan, blockedAction
  - **SubscriptionUsageCard**: Full subscription dashboard with usage progress bars
  - **UpgradePrompt**: Inline upgrade prompts with plan comparison dialog
  - **Bilingual support**: EN/KH translations for all subscription UI
- **Learnings for future iterations**:
  - Subscription model uses unique clientId constraint for one-to-one relation with Client
  - Plan limits use -1 for unlimited (PRO tier)
  - Usage status calculated from actual product/order/customer counts via Prisma
  - Approaching limit threshold at 80% to warn users before hard limit
  - Plan comparison uses allPlans array for consistent display across upgrade dialogs
  - Feature flags in PlanLimits allow conditional UI based on subscription tier
  - Stripe integration fields (stripeCustomerId, stripeSubscriptionId) ready for future payment integration
---

## 2026-01-12 - US-029
- **Implemented**: Audit log for admin actions
- **Files changed**:
  - `prisma/schema.prisma` (modified - added AuditLog model and AuditAction enum)
  - `lib/audit-service.ts` (new file - logging utility functions)
  - `app/api/audit-logs/route.ts` (new file - GET endpoint with filtering/pagination)
  - `app/api/products/route.ts` (modified - added audit logging to CREATE/UPDATE/DELETE)
  - `app/api/orders/route.ts` (modified - added audit logging to CREATE and status UPDATE)
  - `app/api/settings/route.ts` (modified - added audit logging to PUT)
  - `lib/api-hooks.ts` (modified - added AuditLog types and useAuditLogs hook)
  - `lib/i18n.ts` (modified - added auditLog translations EN/KH)
  - `components/pages/audit-logs-page.tsx` (new file - admin audit log UI)
  - `components/sidebar.tsx` (modified - added audit-logs nav item)
  - `components/admin-dashboard.tsx` (modified - integrated AuditLogsPage)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **AuditLog model**: userId, userName, action (AuditAction enum), resource, resourceId, details (JSON), ipAddress, userAgent, createdAt
  - **AuditAction enum**: CREATE, UPDATE, DELETE, LOGIN, LOGOUT, SETTINGS_CHANGE
  - **Audit service helpers**: logAudit (base), logProductAudit, logOrderAudit, logSettingsAudit, logUserAudit, etc.
  - **extractRequestInfo**: Extracts IP address and user agent from request headers
  - **createChangeDiff**: Creates before/after diff for UPDATE actions
  - **GET /api/audit-logs**: Filtering by userId, action, resource, resourceId, date range, search; pagination; summary stats by action/resource/hourly
  - **Summary charts**: PieChart for action distribution, BarChart for resource distribution, AreaChart for hourly activity
  - **AuditLogsPage UI**: Filters card, log list with action icons, pagination, detail dialog with JSON viewer
  - **Non-blocking logging**: Fire-and-forget pattern to avoid impacting API response times
  - **Bilingual support**: EN/KH translations for all UI labels
- **Learnings for future iterations**:
  - Fire-and-forget pattern: `logAudit({...})` without await to not block main operation
  - createChangeDiff skips internal fields (id, createdAt, updatedAt) to focus on business data
  - Prisma groupBy aggregations work well for summary stats
  - Raw SQL query for hourly activity grouping (EXTRACT(HOUR FROM created_at))
  - actionIcons and actionColors maps for visual differentiation
  - ScrollArea component from Shadcn for JSON viewer overflow
  - Audit logs stored indefinitely - may need retention policy for production
---

## 2026-01-12 - US-030
- **Implemented**: PWA offline support
- **Files changed**:
  - `app/manifest.ts` (new file - Next.js PWA manifest)
  - `public/sw.js` (new file - service worker with caching strategies)
  - `app/offline/page.tsx` (new file - offline page with cached products)
  - `lib/use-pwa.ts` (new file - PWA hooks for install prompt, online status)
  - `components/pwa-install-prompt.tsx` (new file - install banner, iOS instructions, update banner)
  - `components/shop-app.tsx` (modified - integrated PWA components)
  - `lib/i18n.ts` (modified - added pwa translations EN/KH)
  - `public/icons/icon-512x512.svg` (new file - PWA icon)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **manifest.ts**: Next.js App Router manifest with name, theme_color, SVG icons, shortcuts
  - **Service worker (sw.js)**: Three caching strategies - networkFirst (API), cacheFirst (static), staleWhileRevalidate (products)
  - **Cache management**: STATIC_CACHE, DYNAMIC_CACHE, PRODUCT_CACHE with versioning
  - **Offline page**: Shows cached products from IndexedDB/Cache API, handles online/offline status
  - **usePWA hook**: Service worker registration, install prompt handling, online status, cache management
  - **PWAInstallPrompt**: Shows after 30 seconds if installable, iOS-specific instructions dialog
  - **PWAUpdateBanner**: Shows when new service worker version available
  - **OfflineIndicator**: Fixed banner when offline
  - **Product caching**: Products cached via postMessage to service worker
  - **Bilingual support**: EN/KH translations for all PWA UI text
- **Learnings for future iterations**:
  - Next.js App Router uses manifest.ts (not public/manifest.json) for typed manifest
  - SVG icons work well for PWA manifest with sizes: "any"
  - Service worker strategies: networkFirst for APIs, cacheFirst for static assets, staleWhileRevalidate for products
  - beforeinstallprompt event must be captured and stored for later use
  - iOS Safari doesn't support beforeinstallprompt, show manual instructions instead
  - Service worker message passing for product caching: postMessage({ type: "CACHE_PRODUCTS", products })
  - Use navigator.onLine for initial status, online/offline events for updates
  - matchMedia("(display-mode: standalone)") to detect if already installed
---

## 2026-01-12 - US-031
- **Implemented**: JWT token authentication with access/refresh token pair
- **Files changed**:
  - `lib/jwt.ts` (modified - added access/refresh token system)
  - `lib/auth-middleware.ts` (modified - supports both Bearer tokens and legacy cookies)
  - `app/api/auth/login/route.ts` (modified - generates token pair, sets refresh-token cookie)
  - `app/api/auth/logout/route.ts` (modified - clears refresh-token cookie)
  - `app/api/auth/refresh/route.ts` (new file - token refresh endpoint)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Access token (15min)**: signAccessToken with userId, roleId, email, role, permissions, type:"access"
  - **Refresh token (7 days)**: signRefreshToken with userId, type:"refresh", optional tokenVersion
  - **generateTokenPair()**: Creates both tokens with expiry timestamps
  - **Token rotation**: Refresh endpoint rotates refresh token when < 1 day until expiry
  - **POST /api/auth/refresh**: Verifies refresh token from httpOnly cookie, returns new access token
  - **verifyAccessToken/verifyRefreshToken**: Separate verification with type checking
  - **isTokenExpiredError()**: Type guard for handling expired tokens
  - **extractBearerToken()**: Parses Authorization: Bearer <token> header
  - **Middleware support**: withAuth tries Bearer token first, falls back to legacy auth-token cookie
  - **AUTH_ERROR_CODES**: Specific error codes (ACCESS_TOKEN_EXPIRED, SESSION_EXPIRED, etc.) for client handling
  - **hasPermission/hasRole**: Helper functions for authorization checks
  - **Backwards compatible**: Legacy session-based auth still works alongside new JWT flow
- **Learnings for future iterations**:
  - Use separate secrets for access and refresh tokens (JWT_REFRESH_SECRET env var)
  - Access tokens are stateless (no DB lookup), refresh tokens require user validation
  - Token rotation on refresh improves security by limiting refresh token lifetime
  - Include permissions in access token to avoid DB lookup on every request
  - Return expiry time in seconds for frontend token refresh scheduling
  - SameSite: "strict" for refresh token cookie, "lax" for legacy auth-token
  - Keep legacy auth flow for backwards compatibility with existing sessions
---

## 2026-01-12 - US-032
- **Implemented**: Role-based access control (RBAC) with granular permissions
- **Files changed**:
  - `prisma/schema.prisma` (modified - added Permission, RolePermission models, updated Role model)
  - `lib/rbac.ts` (new file - RBAC configuration and helpers)
  - `lib/auth-middleware.ts` (modified - added RBAC middleware functions)
  - `app/api/permissions/route.ts` (new file - GET/POST for permissions)
  - `app/api/roles/route.ts` (modified - enhanced with permission management)
  - `lib/api-hooks.ts` (modified - added Permission types and hooks)
  - `components/pages/role-permissions-page.tsx` (new file - admin UI for role permissions)
  - `components/sidebar.tsx` (modified - added role-permissions nav item)
  - `components/admin-dashboard.tsx` (modified - integrated RolePermissionsPage)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Permission model**: id, name (e.g., "products:read"), resource, action, displayName, description
  - **RolePermission junction table**: roleId, permissionId with cascade delete
  - **Role model enhancements**: displayName, description, isSystem, rolePermissions relation
  - **System roles**: super_admin (all permissions), admin (most permissions), manager (operational), staff (limited)
  - **42 granular permissions** across 15 resources: products, orders, customers, inventory, promotions, content, users, roles, settings, reports, categories, payments, reviews, audit_logs, clients
  - **Permission actions**: read, write, delete, export, manage
  - **lib/rbac.ts exports**:
    - SystemRoles, Resources, Actions constants
    - permissionDefinitions with displayName and descriptions
    - defaultRolePermissions for each system role
    - checkPermission, checkAnyPermission, checkAllPermissions helpers
    - convertLegacyPermissions/convertToLegacyFormat for backwards compatibility
    - isSuperAdmin, isSystemRole, getRoleLevel, hasHigherOrEqualRole helpers
  - **New middleware functions** in auth-middleware.ts:
    - requireResourcePermission(resource, action) - checks specific permission
    - requireAnyPermission([...permissions]) - checks any of multiple permissions
    - requireAllPermissions([...permissions]) - checks all permissions
    - requireRole(role) - checks role hierarchy
    - requireSuperAdmin() - super admin only
    - canAccess(user, resource, action) - inline permission check
    - getUserPermissionsArray(user) - converts to array format
  - **GET /api/permissions**: Lists all permissions with byResource grouping
  - **POST /api/permissions**: Seeds permissions from definitions
  - **Enhanced /api/roles**:
    - ?includePermissions=true returns rolePermissions with permission details
    - POST { seed: true } creates system roles with default permissions
    - PUT supports permissionNames array for updating role permissions
    - Protected against deleting/renaming system roles
  - **RolePermissionsPage UI**:
    - Role list with color-coded cards and system badge
    - Permission matrix with collapsible resource sections
    - Toggle all permissions per resource
    - List view of enabled permissions
    - "Seed System Roles" button for initial setup
    - Reset to default permissions for system roles
    - Create custom role dialog
    - Unsaved changes warning
  - **Super admin bypass**: All permission checks return true for super_admin role
  - **Legacy format support**: Converts between object format ({"products": ["read"]}) and array format (["products:read"])
- **Learnings for future iterations**:
  - Junction table (RolePermission) enables many-to-many with full CRUD on the relationship
  - Dual permission format support (legacy object + new array) ensures backwards compatibility
  - Super admin bypass at middleware level simplifies permission logic
  - System roles (isSystem: true) protected from deletion and renaming
  - Permission seeding can be triggered via API for initial setup or after adding new permissions
  - Role hierarchy (getRoleLevel) enables "can manage users of lower role" logic
  - Permission matrix UI uses Set for efficient toggle operations
  - Collapsible sections improve UX when many resources exist
---

## 2026-01-12 - US-033
- **Implemented**: Two-factor authentication (2FA) with TOTP
- **Files changed**:
  - `prisma/schema.prisma` (modified - added twoFactorEnabled, twoFactorSecret, recoveryCodes to User model)
  - `lib/totp.ts` (new file - TOTP utilities using otplib)
  - `lib/jwt.ts` (modified - added pending 2FA token support)
  - `app/api/auth/login/route.ts` (modified - returns pendingToken when 2FA enabled)
  - `app/api/auth/2fa/setup/route.ts` (new file - POST/PUT/DELETE for setup flow)
  - `app/api/auth/2fa/verify/route.ts` (new file - POST to complete login with 2FA)
  - `app/api/auth/2fa/disable/route.ts` (new file - POST to disable 2FA)
  - `app/api/auth/2fa/recovery-codes/route.ts` (new file - GET count, POST regenerate)
  - `app/api/auth/2fa/status/route.ts` (new file - GET 2FA status)
  - `components/two-factor-settings.tsx` (new file - UI component for 2FA management)
  - `lib/i18n.ts` (modified - added twoFactor translations EN/KH)
  - `package.json`, `bun.lock` (modified - added otplib, qrcode dependencies)
- **Features**:
  - **User model 2FA fields**: twoFactorEnabled (boolean), twoFactorSecret (encrypted), recoveryCodes (hashed JSON array)
  - **TOTP utilities** (lib/totp.ts):
    - generateSecret(): Creates TOTP secret
    - verifyTOTP(): Async verification with 30s window
    - generateKeyUri(): Creates otpauth:// URI for authenticator apps
    - generateQRCode(): Creates QR code data URL using qrcode library
    - generateRecoveryCodes(): Creates 10 recovery codes (format: XXXX-XXXX)
    - hashRecoveryCode/verifyRecoveryCode: SHA-256 hashing for storage
    - encryptSecret/decryptSecret: AES-256-GCM encryption for TOTP secrets
  - **Pending 2FA token** (lib/jwt.ts):
    - signPendingToken/verifyPendingToken: 5-minute token for 2FA flow
    - Pending2FATokenPayload type with userId, email, type: "pending_2fa"
  - **Login flow with 2FA**:
    - Returns `{ requires2FA: true, pendingToken, expiresIn }` when 2FA enabled
    - User submits pendingToken + 2FA code to /api/auth/2fa/verify
    - Verify endpoint validates TOTP or recovery code, issues full token pair
  - **2FA setup flow**:
    - POST /api/auth/2fa/setup: Generates secret, QR code, recovery codes
    - PUT /api/auth/2fa/setup: Verifies user can generate valid codes, enables 2FA
    - DELETE /api/auth/2fa/setup: Cancels pending setup
  - **Recovery codes**:
    - 10 codes generated on setup, each can be used once
    - Recovery code removes itself from list after use
    - GET /api/auth/2fa/recovery-codes: Returns remaining count
    - POST /api/auth/2fa/recovery-codes: Regenerates all codes (requires password + TOTP)
  - **TwoFactorSettings component**:
    - Setup wizard: QR code display, manual entry option, code verification
    - Recovery codes display with download/copy options
    - Disable flow with password confirmation
    - Regenerate codes dialog with security verification
    - Status display with low recovery codes warning
  - **Bilingual support**: Complete EN/KH translations for all 2FA UI
- **Learnings for future iterations**:
  - otplib v14+ uses async verify() function, not sync like earlier versions
  - Use OTP class with strategy: "totp" for async operations
  - generateURI() instead of keyuri() in newer otplib versions
  - Pending token pattern enables stateless 2FA verification between login steps
  - Recovery codes should be hashed (not encrypted) since they're one-time use
  - TOTP secrets should be encrypted at rest for security
  - QR code generation is async - use toDataURL from qrcode library
  - 5-minute pending token expiry balances security and UX
  - Show recovery codes only once during setup - never retrievable again
---

## 2026-01-12 - US-034
- **Implemented**: Password security enhancements (OWASP recommended practices)
- **Files changed**:
  - `prisma/schema.prisma` (modified - added PasswordHistory model, passwordChangedAt to User)
  - `lib/password-security.ts` (new file - password validation, strength calculation, bcrypt helpers)
  - `components/password-strength-indicator.tsx` (new file - UI components)
  - `app/api/users/route.ts` (modified - strong validation, history check, bcrypt 12)
  - `app/api/auth/login/route.ts` (modified - password expiry warning in response)
  - `lib/i18n.ts` (modified - added passwordSecurity translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Strong password requirements**: min 8 chars, uppercase, lowercase, number, special char
  - **PASSWORD_CONFIG**: Centralized config for minLength(8), maxLength(128), bcryptCostFactor(12), historyCount(5), expiryDays(90)
  - **passwordRequirements array**: Typed requirements with test functions and bilingual labels
  - **calculatePasswordStrength()**: Returns level (weak/fair/good/strong), score (0-100), labels (EN/KH)
  - **validatePassword()**: Returns isValid, errors, requirements status, strength
  - **hashPassword()/verifyPassword()**: bcrypt with cost factor 12
  - **isPasswordInHistory()**: Checks password against array of historical hashes
  - **Password expiry**: isPasswordExpired(), shouldShowExpiryWarning(), getPasswordAge()
  - **PasswordHistory model**: userId, passwordHash, createdAt with auto-cleanup of old entries
  - **PasswordStrengthIndicator component**: Real-time strength bar, requirements checklist, success message
  - **PasswordStrengthBadge component**: Compact badge for inline use
  - **PasswordExpiryWarning component**: Warning banner with urgency levels and action button
  - **Users API enhancements**: Validates on create/update, stores history, prevents reuse of last 5
  - **Login response**: Includes passwordExpiry info when approaching 90-day limit
  - **Bilingual support**: Full EN/KH translations for all UI text
- **Learnings for future iterations**:
  - bcrypt cost factor 12 is OWASP recommended (seed already used 12, API was using 10)
  - Password history check is O(n) where n = historyCount due to bcrypt compare
  - Keep password requirements as typed objects for reusability in frontend/backend
  - Strength calculation: base score from requirements + bonuses/penalties for patterns
  - Common patterns to penalize: 123, password, qwerty, repeated chars, word+numbers
  - 90-day expiry with 14-day warning window balances security and UX
  - passwordChangedAt is null for legacy accounts - treat as "no expiry" for backwards compatibility
  - PasswordStrengthIndicator shows/hides based on password input for clean UX
---

## 2026-01-12 - US-035
- **Implemented**: Account lockout protection against brute force attacks
- **Files changed**:
  - `prisma/schema.prisma` (modified - added LoginAttempt and AccountLockout models)
  - `lib/account-lockout.ts` (new file - lockout service with tracking and validation)
  - `app/api/auth/login/route.ts` (modified - integrated lockout checks and attempt tracking)
  - `app/api/admin/lockouts/route.ts` (new file - admin API for managing lockouts)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **LoginAttempt model**: email, userId, ipAddress, userAgent, success, createdAt with indexes
  - **AccountLockout model**: userId (unique), lockedAt, lockedUntil, failedAttempts, ipAddress, unlockedAt, unlockedBy
  - **LOCKOUT_CONFIG**: Configurable maxFailedAttempts (5), lockoutDurationMinutes (15), attemptWindowMinutes (15)
  - **extractIpAddress()**: Extracts client IP from x-forwarded-for, x-real-ip, cf-connecting-ip headers
  - **recordLoginAttempt()**: Logs all login attempts (success or fail) with IP and user agent
  - **checkLockoutByEmail()**: Pre-authentication check for locked accounts
  - **handleFailedLogin()**: Records attempt, counts recent failures, triggers lockout at threshold
  - **handleSuccessfulLogin()**: Records success, clears any existing lockout
  - **lockAccount()/unlockAccount()**: Lock management with admin unlock tracking
  - **sendLockoutNotification()**: Email notification when account is locked (fire-and-forget)
  - **GET /api/admin/lockouts**: List locked accounts with summary stats (last 24h/7d lockouts, failed attempts)
  - **GET /api/admin/lockouts?userId=**: Detailed lockout history and recent attempts for specific user
  - **POST /api/admin/lockouts**: Manual unlock with audit logging
  - **Login API integration**: Pre-checks lockout, records attempts, returns attemptsRemaining, 423 status when locked
- **Learnings for future iterations**:
  - HTTP 423 Locked is the appropriate status code for account lockout responses
  - Fire-and-forget pattern for notifications: don't await, let them fail silently
  - Track failed attempts even for non-existent emails to prevent user enumeration attacks
  - Use composite index [email, success, createdAt] for efficient failed attempt counting
  - Lockout by userId (not email) prevents locking multiple accounts sharing email
  - Admin unlock records who unlocked (unlockedBy) for audit trail
  - Include attemptsRemaining in 401 response so frontend can warn users
  - cleanupOldLoginAttempts() helper for periodic maintenance (24h retention)
---

## 2026-01-12 - US-036
- **Implemented**: Session management for tracking and managing active user sessions
- **Files changed**:
  - `prisma/schema.prisma` (modified - enhanced Session model with device/IP/UA/lastActive/isRevoked fields)
  - `lib/session-service.ts` (new file - session CRUD operations, device parsing, activity tracking)
  - `app/api/sessions/route.ts` (new file - GET/DELETE for session list and bulk revoke)
  - `app/api/sessions/[id]/route.ts` (new file - DELETE for individual session revoke)
  - `components/active-sessions.tsx` (new file - UI component for viewing/managing sessions)
  - `lib/api-hooks.ts` (modified - added session types and hooks)
  - `lib/auth-middleware.ts` (modified - added session activity update, isRevoked check)
  - `app/api/auth/login/route.ts` (modified - integrated createSession with device info)
  - `app/api/auth/2fa/verify/route.ts` (modified - integrated createSession with device info)
  - `lib/i18n.ts` (modified - added sessions translations EN/KH)
  - `package.json` (modified - added ua-parser-js dependency)
- **Features**:
  - **Enhanced Session model**: Added deviceName, deviceType, ipAddress, userAgent, location, lastActive, isRevoked, revokedAt, isCurrent fields
  - **Session service (lib/session-service.ts)**:
    - SESSION_CONFIG with 30-day sessionDuration and inactivityThreshold
    - parseUserAgent() using ua-parser-js for device detection
    - extractIpAddress() from various proxy headers
    - createSession() with device info capture
    - touchSession()/touchSessionByToken() for activity updates
    - getUserSessions() returns formatted session list
    - revokeSession()/revokeAllSessionsExcept()/revokeAllSessions() for session management
    - validateSession() checks expiry, revocation, and inactivity
    - cleanupExpiredSessions() for maintenance
    - getSessionStats() for session analytics
  - **GET /api/sessions**: Returns active sessions with device icons, location, lastActive timestamps
  - **DELETE /api/sessions**: "Log out all devices" - revokes all except current
  - **DELETE /api/sessions/[id]**: Revoke specific session
  - **ActiveSessions component**: Card-based UI with device icons (Monitor/Smartphone/Tablet/Globe), relative time display, current session badge, revoke confirmation dialogs, security tip warning
  - **Auth integration**: Login and 2FA verify flows now create enhanced sessions with device info
  - **Activity tracking**: Auth middleware updates lastActive on each authenticated request
  - **Auto-expiry**: validateSession() checks 30-day inactivity threshold
  - **Hooks**: useSessions, useRevokeSession, useRevokeAllSessions in api-hooks.ts
  - **Bilingual support**: Full EN/KH translations for session management UI
- **Learnings for future iterations**:
  - ua-parser-js v2+ uses new API patterns - check documentation for latest syntax
  - Session activity should be fire-and-forget (touchSession) to not block API responses
  - isRevoked + revokedAt pattern enables soft-delete with audit trail
  - Device parsing from User-Agent: browser name + OS name = deviceName
  - Relative time formatting: use thresholds (minutes -> hours -> days) for human-readable times
  - Session model isCurrent field marked at runtime, not stored - compare sessionId in response
  - IP extraction priority: x-forwarded-for > x-real-ip > cf-connecting-ip
  - AlertDialog from Shadcn works well for confirmation flows (revoke session, log out all)
---

## 2026-01-12 - US-037
- **Implemented**: Security headers middleware
- **Files changed**:
  - `middleware.ts` (modified - added security headers to all responses)
  - `lib/security-headers.ts` (new file - reusable security header utilities)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Content-Security-Policy (CSP)**: Comprehensive CSP with directives for default-src, script-src (with unsafe-inline for Next.js, unsafe-eval only in dev), style-src, img-src (self, data, blob, https), font-src, connect-src (with WebSocket in dev, Telegram API), media-src, object-src (none), frame-ancestors (none), form-action, base-uri, manifest-src, worker-src, child-src
  - **X-Content-Type-Options**: nosniff - prevents MIME type sniffing attacks
  - **X-Frame-Options**: DENY - prevents clickjacking by blocking iframe embedding
  - **Strict-Transport-Security (HSTS)**: max-age=31536000 (1 year) with includeSubDomains and preload flags (production only, max-age=0 in dev)
  - **X-XSS-Protection**: 1; mode=block - legacy browser XSS protection
  - **Permissions-Policy**: Controls browser features - allows camera, geolocation, payment, fullscreen for self; disables microphone, USB, display-capture, accelerometer, gyroscope, magnetometer; opts out of FLoC
  - **Referrer-Policy**: strict-origin-when-cross-origin - controls referrer information
  - **X-DNS-Prefetch-Control**: on - enables DNS prefetching for performance
  - **lib/security-headers.ts**: Standalone module with generateCSP(), generatePermissionsPolicy(), getSecurityHeaders(), applySecurityHeaders(), getSecurityHeadersArray() for Next.js config, generateNonce() for future CSP nonces
- **Learnings for future iterations**:
  - Next.js middleware applies to all matched routes, making it ideal for security headers
  - HSTS should only be enabled in production to avoid localhost HTTPS issues
  - CSP unsafe-inline is required for Next.js inline scripts and Tailwind CSS
  - CSP unsafe-eval is needed in development for hot reload but should be removed in production
  - Permissions-Policy replaces the deprecated Feature-Policy header
  - interest-cohort=() opts out of Google's FLoC tracking
  - frame-ancestors in CSP is modern replacement for X-Frame-Options but both included for compatibility
  - Middleware runs at edge runtime, so keep functions simple (no complex imports)
---

## 2026-01-12 - US-038
- **Implemented**: CSRF protection for forms
- **Files changed**:
  - `lib/csrf.ts` (new file - CSRF token generation, validation, and utilities)
  - `app/api/csrf/route.ts` (new file - GET/POST endpoints for CSRF tokens)
  - `lib/auth-middleware.ts` (modified - added withCSRF, withAuthAndCSRF, validateCSRF, getCSRFToken)
  - `lib/use-csrf.ts` (new file - React hook for CSRF token management)
  - `lib/i18n.ts` (modified - added csrf translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **CSRF token generation**: HMAC-SHA256 signed tokens tied to session ID
  - **Token format**: base64url encoded `{sessionId}:{random}:{timestamp}:{signature}`
  - **Token expiry**: 1 hour default, configurable via CSRF_CONFIG
  - **Session binding**: Token tied to first 32 chars of auth token (JWT or session)
  - **Timing-safe validation**: Prevents timing attacks during signature comparison
  - **GET /api/csrf**: Returns new token with expiry time, sets HttpOnly=false cookie for JS access
  - **POST /api/csrf**: Validates token for testing purposes
  - **withCSRF middleware**: Validates CSRF on POST/PUT/DELETE/PATCH, skips GET/HEAD/OPTIONS
  - **withAuthAndCSRF middleware**: Combined auth + CSRF validation for protected routes
  - **Auto-refresh mechanism**: Refreshes token when <15min remaining, returns new token via X-CSRF-Token-Refresh header
  - **Error codes**: CSRF_MISSING, CSRF_INVALID, CSRF_EXPIRED with appropriate HTTP status (403/419)
  - **useCSRF React hook**: Token management, auto-fetch, getHeaders(), handleResponse(), invalidate()
  - **createCSRFFetch wrapper**: Automatically includes CSRF token in requests, handles refreshed tokens
  - **CSRFError class**: Custom error type for CSRF failures with isCSRFError() type guard
  - **Form utilities**: addCSRFToFormData(), addCSRFToBody() helpers
  - **Bilingual support**: EN/KH translations for error messages
- **Learnings for future iterations**:
  - CSRF tokens must NOT be HttpOnly - JavaScript needs access to include in AJAX headers
  - Use SameSite=Strict for CSRF cookie to prevent cross-site submission
  - Token tied to auth session ensures token changes on login/logout
  - Timing-safe string comparison prevents attackers from learning signature byte-by-byte
  - 419 status code (Laravel convention) distinguishes token expiry from other 403 errors
  - Auto-refresh via response headers allows seamless token rotation without extra requests
  - Session ID extraction works with both JWT (first 32 chars of Bearer token) and legacy cookies
  - CSRF_SECRET should be separate from JWT_SECRET but can share as fallback
---

## 2026-01-12 - US-039
- **Implemented**: API key authentication for third-party integrations
- **Files changed**:
  - `prisma/schema.prisma` (modified - added ApiKey and ApiKeyUsageLog models)
  - `lib/api-key.ts` (new file - key generation, validation, rate limiting, scopes)
  - `app/api/keys/route.ts` (new file - GET/POST/PUT/DELETE endpoint for key management)
  - `lib/auth-middleware.ts` (modified - added withApiKey, requireApiKeyScope, ApiKeyContext)
  - `lib/api-hooks.ts` (modified - added ApiKey types and hooks)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **ApiKey model**: id, clientId, name, keyHash (SHA-256), keyPrefix (for identification), scopes (JSON array), rateLimitPerMinute, lastUsedAt, usageCount, expiresAt, previousKeyHash/previousKeyExpiresAt (for rotation)
  - **ApiKeyUsageLog model**: Tracks endpoint, method, ipAddress, userAgent, statusCode, responseTime per request
  - **12 scopes across 8 resources**: products (read/write/delete), orders (read/write), customers (read/write), inventory (read/write), categories (read/write), settings (read), analytics (read), webhooks (write)
  - **SCOPE_PRESETS**: readonly, standard, full for quick scope selection
  - **Key generation**: `ak_<random>` format with SHA-256 hashing for storage
  - **Key validation**: Timing-safe comparison, expiry check, active status check
  - **Rate limiting**: In-memory rate limiter with per-key limits (default 60/min, max 1000/min)
  - **Rate limit headers**: X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset
  - **Key rotation**: rotateApiKey() preserves old key for 24h grace period
  - **Usage tracking**: Fire-and-forget logging of every API key request
  - **withApiKey middleware**: Full API key authentication with rate limiting
  - **requireApiKeyScope/requireAnyApiKeyScope/requireAllApiKeyScopes**: Scope-based authorization
  - **withAuthOrApiKey middleware**: Supports both user auth and API key auth
  - **GET /api/keys**: List keys (masked), pagination, available scopes, scope presets
  - **POST /api/keys**: Create key (returns full key ONCE, with warning)
  - **PUT /api/keys**: Update settings or rotate key
  - **DELETE /api/keys**: Soft disable or permanent delete
  - **Hooks**: useApiKeys, useCreateApiKey, useUpdateApiKey, useRotateApiKey, useDeleteApiKey
- **Learnings for future iterations**:
  - API keys use SHA-256 hash (not bcrypt) since we never need to verify partial keys
  - Key shown only once on creation - important to warn users to save it
  - Key prefix (first 8 chars) stored separately for identification in UI
  - In-memory rate limiting works for single server but needs Redis for distributed
  - Key rotation with grace period allows seamless transition without downtime
  - Fire-and-forget pattern for usage logging avoids blocking API responses
  - Scope format "action:resource" (e.g., "read:products") is intuitive and matches common patterns
  - X-API-Key header as alternative to Authorization: Bearer for compatibility
  - Timing-safe comparison prevents timing attacks on key validation
---

## 2026-01-12 - US-040
- **Implemented**: OAuth2 social login with Google and Facebook
- **Files changed**:
  - `prisma/schema.prisma` (modified - added SocialAccount model, OAuthProvider enum)
  - `lib/oauth.ts` (new file - OAuth configuration and utilities)
  - `lib/oauth-service.ts` (new file - account linking/creation/login service)
  - `app/api/auth/oauth/route.ts` (new file - list available providers)
  - `app/api/auth/oauth/google/route.ts` (new file - initiate Google OAuth)
  - `app/api/auth/oauth/google/callback/route.ts` (new file - Google callback handler)
  - `app/api/auth/oauth/facebook/route.ts` (new file - initiate Facebook OAuth)
  - `app/api/auth/oauth/facebook/callback/route.ts` (new file - Facebook callback handler)
  - `app/api/auth/social-accounts/route.ts` (new file - manage linked accounts)
  - `lib/api-hooks.ts` (modified - added OAuth types and hooks)
  - `lib/i18n.ts` (modified - added oauth translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **SocialAccount model**: provider (GOOGLE/FACEBOOK), providerUserId, email, name, avatarUrl, tokens (encrypted), rawProfile
  - **OAuth config** (lib/oauth.ts):
    - OAUTH_CONFIG with Google and Facebook provider settings
    - generateOAuthState/verifyOAuthState with HMAC signature and expiry (10 min)
    - getGoogleAuthUrl/getFacebookAuthUrl for authorization URLs
    - exchangeGoogleCode/exchangeFacebookCode for token exchange
    - fetchGoogleProfile/fetchFacebookProfile for user data
    - encryptOAuthToken/decryptOAuthToken with AES-256-GCM
    - getAvailableProviders/isProviderConfigured helpers
  - **OAuth service** (lib/oauth-service.ts):
    - processOAuthLogin: Complete login/signup flow
    - Flow: Check existing social account -> Link by email -> Create new customer
    - linkSocialAccountToUser/linkSocialAccountToCustomer for account linking
    - unlinkSocialAccount for disconnecting providers
    - getUserSocialAccounts/getCustomerSocialAccounts for listing
  - **OAuth API endpoints**:
    - GET /api/auth/oauth - list enabled providers
    - GET /api/auth/oauth/google - redirect to Google OAuth
    - GET /api/auth/oauth/google/callback - handle Google callback
    - GET /api/auth/oauth/facebook - redirect to Facebook OAuth
    - GET /api/auth/oauth/facebook/callback - handle Facebook callback
    - GET/DELETE /api/auth/social-accounts - manage linked accounts
  - **React hooks**: useOAuthProviders, useSocialAccounts, useUnlinkSocialAccount
  - **Helper**: initiateOAuthLogin(provider, options) for easy OAuth initiation
  - **Env vars**: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, FACEBOOK_APP_ID, FACEBOOK_APP_SECRET, OAUTH_STATE_SECRET
  - **Bilingual support**: Full EN/KH translations for OAuth UI
- **Learnings for future iterations**:
  - OAuth state parameter uses HMAC-SHA256 signature with timestamp for CSRF protection
  - State encodes provider, returnUrl, and optional linkUserId/linkCustomerId for account linking
  - Token encryption uses AES-256-GCM with random IV stored alongside ciphertext
  - Facebook Graph API v19.0 requires explicit fields parameter for user data
  - Google returns refresh_token only on first authorization (requires prompt=consent)
  - Social account linking checks both userId and customerId to prevent cross-linking
  - Email matching automatically links social account to existing user/customer
  - Customer phone is required in schema - use oauth_{provider}_{userId} placeholder for OAuth-created customers
  - OAuth login creates session using existing session-service for consistency
  - Callback routes set oauth-login cookie (1 min expiry) for client-side success detection
---

## 2026-01-12 - US-041
- **Implemented**: Password reset flow
- **Files changed**:
  - `prisma/schema.prisma` (modified - added PasswordResetToken model, relation to User)
  - `lib/password-reset.ts` (new file - password reset service)
  - `app/api/auth/forgot-password/route.ts` (new file - POST endpoint)
  - `app/api/auth/reset-password/route.ts` (new file - GET/POST endpoint)
  - `lib/i18n.ts` (modified - added passwordReset translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **PasswordResetToken model**: userId, token (SHA-256 hashed), expiresAt, usedAt, ipAddress, userAgent
  - **RESET_CONFIG**: tokenExpiryMinutes (60), maxRequestsPerHour (3), tokenLength (32), minTimeBetweenRequests (60s)
  - **Token generation**: Secure random token with SHA-256 hashing for storage
  - **Rate limiting**: 3 requests per hour per email, 60-second cooldown between requests
  - **POST /api/auth/forgot-password**: Validates email, checks rate limit, generates token, sends email
  - **GET /api/auth/reset-password**: Validates token without exposing userId (just email)
  - **POST /api/auth/reset-password**: Validates token, new password strength, password history
  - **Password validation**: Uses existing PASSWORD_CONFIG (min 8 chars, complexity requirements)
  - **Password history check**: Prevents reuse of last 5 passwords via isPasswordInHistory()
  - **Session revocation**: All sessions invalidated on password reset (force re-login)
  - **Email notifications**: Reset link email and confirmation email via notification-service
  - **Security measures**: Generic responses to prevent email enumeration, timing-safe token comparison
  - **Bilingual support**: Full EN/KH translations for all UI text
- **Learnings for future iterations**:
  - SHA-256 for token storage (not bcrypt) since we only need exact match, not partial verification
  - Generic "If account exists" response prevents user enumeration attacks
  - Token invalidation on password reset ensures one-time use
  - Rate limit by email (not IP) to prevent distributed attacks while allowing legitimate retries
  - Cooldown period (minTimeBetweenRequests) prevents rapid request spam
  - Session revocation is critical security measure - user must re-authenticate
  - Password history stored indefinitely (historyCount controls how many to check, not total stored)
  - Clean up expired/used tokens periodically via cleanupExpiredTokens()
---

## 2026-01-12 - US-042
- **Implemented**: Email verification
- **Files changed**:
  - `prisma/schema.prisma` (modified - added EmailVerifyToken model, emailVerified/emailVerifiedAt to User/Customer)
  - `lib/email-verification.ts` (new file - verification service)
  - `app/api/auth/send-verification/route.ts` (new file - GET/POST endpoint)
  - `app/api/auth/verify-email/route.ts` (new file - GET/POST endpoint)
  - `lib/i18n.ts` (modified - added emailVerification translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **EmailVerifyToken model**: userId (optional), customerId (optional), email, token (SHA-256 hashed), expiresAt, usedAt, ipAddress, userAgent
  - **User/Customer model additions**: emailVerified (boolean), emailVerifiedAt (datetime)
  - **VERIFICATION_CONFIG**: tokenExpiryHours (24), maxResendPerHour (3), minTimeBetweenResends (60s)
  - **sendUserVerificationEmail()**: Generates token, sends email, rate limits
  - **sendCustomerVerificationEmail()**: Same for customer accounts
  - **validateVerificationToken()**: Validates without consuming (for preview)
  - **verifyEmail()**: Consumes token, updates emailVerified status
  - **getVerificationStatus()**: Returns verification state and resend eligibility
  - **isEmailVerificationRequired()**: Helper to check if action requires verification
  - **GET /api/auth/send-verification**: Returns verification status
  - **POST /api/auth/send-verification**: Sends/resends verification email
  - **GET /api/auth/verify-email**: Redirects to success/error page after verification
  - **POST /api/auth/verify-email**: JSON API for AJAX verification
  - **Bilingual support**: Full EN/KH translations for all verification UI
- **Learnings for future iterations**:
  - EmailVerifyToken supports both userId and customerId (one must be set)
  - 24-hour token expiry is more lenient than password reset (1 hour) since email change is less critical
  - Rate limit is per-account (not per-email) since we're verifying known accounts
  - GET endpoint redirects to pages, POST returns JSON - supports both link clicks and SPA
  - validateVerificationToken() allows checking token without consuming (useful for preview before confirm)
  - isEmailVerificationRequired() centralizes list of restricted actions
  - cleanupExpiredTokens() can be run periodically to clean database
---

## 2026-01-12 - US-043
- **Implemented**: Login activity notifications
- **Files changed**:
  - `prisma/schema.prisma` (modified - added LoginActivity model, loginNotificationsEnabled to User)
  - `lib/login-activity.ts` (new file - login activity service)
  - `app/api/auth/login-activity/route.ts` (new file - user activity management)
  - `app/api/admin/security/suspicious-logins/route.ts` (new file - admin review)
  - `app/api/auth/login/route.ts` (modified - integrated processLoginActivity)
  - `app/api/auth/2fa/verify/route.ts` (modified - integrated processLoginActivity)
  - `lib/i18n.ts` (modified - added loginActivity translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **LoginActivity model**: userId, sessionId, deviceName, deviceType, browser, os, ipAddress, country, city, region, isNewDevice, isNewLocation, notificationSent, markedSuspicious, suspiciousAt, reviewedAt, reviewNotes
  - **User model**: Added loginNotificationsEnabled boolean field (default true)
  - **recordLoginActivity()**: Records login with device/location info, detects new device/location
  - **processLoginActivity()**: Main function to call after login, handles notification logic
  - **sendLoginNotification()**: Sends email notification with device/location details
  - **getUserLoginActivities()**: Get user's login history
  - **markLoginAsSuspicious()**: Mark login as "not me"
  - **reviewSuspiciousLogin()**: Admin action to review suspicious logins
  - **getSuspiciousLoginActivities()**: Admin endpoint for suspicious login list
  - **updateLoginNotificationPreference()**: Toggle notification preference
  - **GET /api/auth/login-activity**: User's login activity history + notification preference
  - **POST /api/auth/login-activity**: Mark as suspicious OR update notification preference
  - **GET /api/admin/security/suspicious-logins**: Admin list with stats (total, unreviewed)
  - **POST /api/admin/security/suspicious-logins**: Admin review suspicious login
  - **Bilingual support**: Full EN/KH translations including email notification text
- **Learnings for future iterations**:
  - Fire-and-forget pattern for processLoginActivity() to not block login response
  - Device detection via parseUserAgent() from existing session-service.ts
  - New device/location detection looks back 30 days (LOGIN_ACTIVITY_CONFIG.historyDays)
  - Notification cooldown of 24 hours prevents spam for same device/location
  - Admin review endpoint requires admin role via requireRole("admin")
  - Security recommendations returned with "not me" reports to guide users
  - Country/city detection would require external IP geolocation service (not implemented - fields prepared)
---

## 2026-01-12 - US-044
- **Implemented**: IP whitelisting for admin access
- **Files changed**:
  - `prisma/schema.prisma` (added allowedIPs field to User model)
  - `lib/ip-whitelist.ts` (new file - IP validation and CIDR support)
  - `lib/auth-middleware.ts` (added IP whitelist middleware)
  - `app/api/admin/ip-whitelist/route.ts` (new file - admin API)
- **Features**:
  - `allowedIPs` JSON field on User model (array of IP/CIDR strings)
  - IPv4 and IPv6 address validation
  - CIDR notation support for IP ranges (e.g., "192.168.1.0/24")
  - `withIPWhitelist` middleware for admin routes
  - Super admin users bypass IP restrictions entirely
  - Blocked access attempts logged to audit_logs table
  - GET/POST/DELETE /api/admin/ip-whitelist for management
- **Learnings for future iterations**:
  - IP from request via `x-forwarded-for`, `x-real-ip`, `cf-connecting-ip` headers
  - CIDR matching uses bitwise operations for efficient comparison
  - JWT tokens don't include allowedIPs (fetched from session or DB lookup)
  - Empty allowedIPs array = all IPs allowed (whitelist disabled)
  - Combined middleware patterns: `withAuthAndIPWhitelist`, `withAuthIPWhitelistAndCSRF`
---

## 2026-01-12 - US-045
- **Implemented**: Security event logging
- **Files changed**:
  - `prisma/schema.prisma` (added SecurityLog model, SecurityEventType and SecurityEventSeverity enums)
  - `lib/security-log.ts` (new file - security event logging service)
  - `app/api/security-logs/route.ts` (new file - super admin only endpoint)
  - `app/api/auth/login/route.ts` (modified - added security logging)
  - `app/api/auth/logout/route.ts` (modified - added security logging)
  - `app/api/auth/2fa/verify/route.ts` (modified - added security logging)
  - `app/api/auth/2fa/setup/route.ts` (modified - added security logging)
  - `app/api/auth/2fa/disable/route.ts` (modified - added security logging)
  - `app/api/auth/forgot-password/route.ts` (modified - added security logging)
  - `app/api/auth/reset-password/route.ts` (modified - added security logging)
  - `app/api/sessions/route.ts` (modified - added security logging)
  - `app/api/sessions/[id]/route.ts` (modified - added security logging)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **SecurityLog model**: id, event (SecurityEventType), severity (SecurityEventSeverity), userId, userName, userEmail, targetUserId, targetUserName, ipAddress, userAgent, country, city, details (JSON), sessionId, expiresAt (90 days), createdAt
  - **SecurityEventType enum (40+ events)**:
    - Authentication: LOGIN_SUCCESS, LOGIN_FAILED, LOGIN_2FA_SUCCESS, LOGIN_2FA_FAILED, LOGOUT
    - Session: SESSION_CREATED, SESSION_REVOKED, SESSION_EXPIRED, ALL_SESSIONS_REVOKED
    - Password: PASSWORD_CHANGED, PASSWORD_RESET_REQUESTED, PASSWORD_RESET_COMPLETED, PASSWORD_RESET_FAILED
    - 2FA: TWO_FACTOR_ENABLED, TWO_FACTOR_DISABLED, TWO_FACTOR_RECOVERY_USED, TWO_FACTOR_RECOVERY_REGENERATED
    - Account: ACCOUNT_CREATED, ACCOUNT_UPDATED, ACCOUNT_LOCKED, ACCOUNT_UNLOCKED, ACCOUNT_DELETED, EMAIL_VERIFIED, EMAIL_VERIFICATION_REQUESTED
    - Permissions: ROLE_ASSIGNED, ROLE_REMOVED, PERMISSION_GRANTED, PERMISSION_REVOKED, ROLE_CREATED, ROLE_UPDATED, ROLE_DELETED
    - API Keys: API_KEY_CREATED, API_KEY_ROTATED, API_KEY_REVOKED, API_KEY_DELETED
    - OAuth: OAUTH_ACCOUNT_LINKED, OAUTH_ACCOUNT_UNLINKED, OAUTH_LOGIN_SUCCESS, OAUTH_LOGIN_FAILED
    - Security: SUSPICIOUS_LOGIN_DETECTED, SUSPICIOUS_LOGIN_REVIEWED, IP_BLOCKED, IP_UNBLOCKED
    - Admin: ADMIN_IMPERSONATION_START, ADMIN_IMPERSONATION_END
  - **SecurityEventSeverity enum**: INFO, WARNING, CRITICAL
  - **SECURITY_EVENT_CONFIG**: Maps event types to default severity and description
  - **lib/security-log.ts functions**:
    - logSecurityEvent() - main fire-and-forget logging function
    - logSecurityEventWithRequest() - convenience wrapper with request context
    - Helper functions: logLoginSuccess, logLoginFailed, log2FASuccess, log2FAFailed, logLogout, logPasswordChanged, logPasswordResetRequested, logPasswordResetCompleted, log2FAEnabled, log2FADisabled, logRecoveryCodeUsed, logAccountLocked, logAccountUnlocked, logRoleAssigned, logPermissionChange, logSuspiciousLogin, logApiKeyEvent, logSessionEvent
    - cleanupExpiredSecurityLogs() - 90-day retention cleanup
    - getSecurityLogStats() - summary stats for dashboard
    - detectSuspiciousPatterns() - alerts for suspicious activity patterns
  - **GET /api/security-logs**:
    - Super admin only (via SUPER_ADMIN_KEY header)
    - Filtering: event type, severity, category, userId, ipAddress, date range, search
    - Pagination with limit (max 100)
    - Event categories for grouping: authentication, session, password, twoFactor, account, permissions, apiKeys, oauth, security, admin
    - Summary stats: byEvent, bySeverity, hourlyActivity, criticalLast24h
    - Actions: cleanup (delete expired), stats (get stats), alerts (detect suspicious patterns)
    - Event descriptions for UI display
    - Retention config returned (90 days)
  - **Auth flow integrations**:
    - Login: LOGIN_SUCCESS on success, LOGIN_FAILED on failure, ACCOUNT_LOCKED when locked
    - 2FA: LOGIN_2FA_SUCCESS/LOGIN_2FA_FAILED, TWO_FACTOR_RECOVERY_USED when recovery code used
    - Logout: LOGOUT event
    - 2FA setup: TWO_FACTOR_ENABLED when verified
    - 2FA disable: TWO_FACTOR_DISABLED
    - Password reset: PASSWORD_RESET_REQUESTED, PASSWORD_RESET_COMPLETED, PASSWORD_RESET_FAILED
    - Sessions: SESSION_REVOKED, ALL_SESSIONS_REVOKED
- **Learnings for future iterations**:
  - Fire-and-forget pattern for security logging (don't await, let failures fail silently)
  - Separate event and severity enums for flexible querying
  - Target user context (targetUserId/targetUserName) for admin actions affecting other users
  - 90-day retention via expiresAt field allows efficient cleanup with index
  - Event categories group related events for UI filtering
  - Suspicious pattern detection checks last 24h/1h for failed logins, 2FA failures, password reset spam, lockouts
  - SECURITY_EVENT_CONFIG provides human-readable descriptions and default severity per event type
  - Super admin check uses SUPER_ADMIN_KEY env var with development fallback
---

## 2026-01-12 - US-046
- **Implemented**: Data encryption at rest for PII fields
- **Files changed**:
  - `lib/encryption.ts` (new file - AES-256-GCM encryption utilities)
  - `lib/prisma.ts` (modified - added transparent encryption middleware)
  - `app/api/admin/encryption/route.ts` (new file - encryption status and key rotation API)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **AES-256-GCM encryption**: Uses IV (16 bytes), auth tag (16 bytes), scrypt key derivation
  - **Encrypted format**: `enc:keyId:iv:authTag:ciphertext` (all hex encoded)
  - **ENCRYPTED_FIELDS config**: Defines which fields to encrypt per model:
    - Customer: phone, email, notes
    - Payment: transactionId
  - **lib/encryption.ts functions**:
    - encrypt(): Encrypts plaintext with current key, returns formatted string
    - decrypt(): Decrypts with key rotation support (tries current key, then previous)
    - isEncrypted(): Checks if value has encryption prefix
    - reEncrypt(): Re-encrypts value with current key (for rotation)
    - encryptFields()/decryptFields(): Batch field encryption/decryption
    - getEncryptedFieldsForModel(): Get encrypted fields for a model
    - hasEncryptedFields(): Check if model has encrypted fields
    - isEncryptionConfigured(): Check if ENCRYPTION_KEY is set (32+ chars)
    - getEncryptionStatus(): Returns config status for admin dashboard
    - isKeyRotationNeeded(): Check if previous key exists for rotation
  - **Prisma middleware** (lib/prisma.ts):
    - Transparent encryption on create/update/upsert/createMany/updateMany
    - Transparent decryption on findUnique/findFirst/findMany
    - Handles nested data structures
    - Graceful degradation when encryption not configured
  - **GET /api/admin/encryption**: Super admin only, returns encryption status, rotation needed, model stats
  - **POST /api/admin/encryption**: Super admin only, action: "rotate" to re-encrypt all data with current key
  - **Key rotation capability**:
    - Set ENCRYPTION_KEY_PREVIOUS to old key before changing ENCRYPTION_KEY
    - Increment ENCRYPTION_KEY_ID to track key version
    - POST /api/admin/encryption { action: "rotate" } re-encrypts all records
    - Rotation uses raw SQL to bypass middleware encryption
    - Audit logging for key rotation operations
- **Environment variables**:
  - ENCRYPTION_KEY: Primary encryption key (required, min 32 chars)
  - ENCRYPTION_KEY_PREVIOUS: Previous key for rotation (optional)
  - ENCRYPTION_KEY_ID: Key version identifier (optional, default "1")
- **Learnings for future iterations**:
  - Prisma middleware intercepts all DB operations, ideal for transparent encryption
  - Key rotation needs direct SQL to bypass middleware (avoid double-encryption)
  - scryptSync with salt ensures key derivation even with short input
  - Auth tag verification in GCM mode provides integrity protection
  - Encrypted values are longer than plaintext (prefix + IV + authTag + ciphertext)
  - Graceful degradation: if no key configured, data flows through unencrypted
  - Model stats query needed to count encrypted vs unencrypted records
  - Super admin only access for encryption management (sensitive operation)
---

## 2026-01-12 - US-047
- **Implemented**: Global API rate limiting
- **Files changed**:
  - `lib/rate-limiter.ts` (new file - core rate limiting logic)
  - `lib/auth-middleware.ts` (modified - added rate limiting middleware)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Default limit**: 100 requests per minute per IP for anonymous users
  - **User-type limits**: anonymous (100/min), authenticated (200/min), admin (500/min), super_admin (1000/min)
  - **Endpoint-specific limits**: Configurable overrides for sensitive endpoints (login: 10/min, forgot-password: 3/min, etc.)
  - **In-memory store**: Clean architecture with Redis-ready interface for distributed deployments
  - **429 response**: Returns proper error with Retry-After header and X-RateLimit-* headers
  - **Middleware**: withRateLimit, withAuthAndRateLimit, withRateLimitAndCSRF, withAuthRateLimitAndCSRF
  - **Helper functions**: extractClientIP, getUserTypeFromRole, getEndpointRateLimit
- **Learnings for future iterations**:
  - Rate limiter uses sliding window algorithm via in-memory Map with periodic cleanup
  - IRateLimiter interface allows swapping in-memory for Redis without changing middleware
  - User type detected from request.user (set by withAuth middleware) for authenticated limits
  - Endpoint-specific limits only apply to non-admin users (admins/super_admins use their higher base limits)
  - X-RateLimit-Reset is Unix timestamp in seconds for standard compliance
---

## 2026-01-12 - US-048
- **Implemented**: Input validation middleware using Zod
- **Files changed**:
  - `lib/validation.ts` (new file - core validation middleware)
  - `docs/features/input-validation-middleware.md` (new file - feature documentation)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **withBodyValidation**: HOF for validating JSON request bodies with Zod schemas
  - **withQueryValidation**: HOF for validating URL query parameters
  - **withParamsValidation**: HOF for validating path parameters (Next.js dynamic routes)
  - **withValidation**: Combined body + query validation
  - **XSS sanitization**: sanitizeString, containsXSS, sanitizeObject functions
  - **XSS patterns detected**: script tags, javascript: protocol, event handlers, iframes, style tags, etc.
  - **Type-safe requests**: ValidatedBodyRequest<T>, ValidatedQueryRequest<T>, ValidatedParamsRequest<T>
  - **Common schemas**: paginationSchema, searchSchema, dateRangeSchema, sortSchema, commonQuerySchema
  - **Safe string types**: safeString (auto-sanitize), noXSSString (reject XSS), safeEmail, safeUrl, safePhone
  - **Error response format**: Detailed 400 response with code, details (field/message/code), and summary
  - **Error codes**: INVALID_BODY, INVALID_QUERY, INVALID_PARAMS, BODY_REQUIRED, INVALID_JSON, XSS_DETECTED
- **Learnings for future iterations**:
  - Zod's safeParse returns {success: boolean, data/error} for non-throwing validation
  - error.flatten() provides convenient fieldErrors and formErrors structure
  - XSS patterns must reset lastIndex for global regex after test() call
  - Next.js 15+ uses async params - must await context.params in dynamic routes
  - HTML entity encoding (&lt;, &gt;) is optional - stripping tags is often sufficient
  - Validation middleware composes well with auth middleware (withAuth(withBodyValidation(...)))
---

## 2026-01-12 - US-049
- **Implemented**: Secure file upload with magic byte validation
- **Files changed**:
  - `lib/secure-upload.ts` (new file - comprehensive file upload security utilities)
  - `app/api/upload/route.ts` (modified - enhanced with security validation)
  - `app/api/uploads/signed-url/route.ts` (new file - generate signed URLs)
  - `app/api/uploads/signed-url/verify/route.ts` (new file - verify and redirect)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - Magic byte validation for JPEG, PNG, WebP, GIF, AVIF, SVG, PDF
  - Special handling for WebP (RIFF container) and AVIF (ftyp box)
  - Type-specific max file sizes (e.g., 10MB for images, 1MB for SVG, 20MB for PDF)
  - UUID v4 secure filename generation using crypto.randomUUID()
  - HMAC-SHA256 signed URLs with configurable expiry (default 1 hour)
  - SVG XSS validation (checks for script tags, event handlers, etc.)
  - ClamAV malware scanning placeholder (ready for integration)
  - Comprehensive validateFile() function combining all checks
  - Audit logging for upload attempts (success/failure)
- **Learnings for future iterations**:
  - Magic bytes need special handling for container formats (RIFF for WebP, ftyp for AVIF/HEIF)
  - SVG files are text-based, need XSS sanitization before allowing
  - Signed URLs use HMAC with timing-safe comparison for security
  - Use crypto.timingSafeEqual() to prevent timing attacks on signature verification
  - Type detection should use detected type for storage, not client-declared type
  - Consider fail-open vs fail-closed for ClamAV - currently fail-open with logging
---

## 2026-01-12 - US-050
- **Implemented**: Admin security dashboard for real-time security monitoring
- **Files changed**:
  - `app/api/admin/security-dashboard/route.ts` (new file - security dashboard API)
  - `components/pages/security-dashboard-page.tsx` (new file - dashboard UI component)
  - `components/sidebar.tsx` (modified - added security-dashboard nav item)
  - `components/admin-dashboard.tsx` (modified - integrated SecurityDashboardPage)
  - `lib/api-hooks.ts` (modified - added useSecurityDashboard, useUnlockAccount, useForceLogout hooks and types)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **GET /api/admin/security-dashboard**: Returns comprehensive security metrics with timeframe filter (1h/6h/24h/7d/30d)
  - **Security score (0-100)**: Calculated from failed login rate, locked accounts, critical events, suspicious logins
  - **Summary metrics**: loginSuccessCount, loginFailedCount, failedLoginRate, activeSessions, lockedAccountsCount, unreviewedSuspiciousLogins, criticalEventsCount
  - **Sessions breakdown**: Total active sessions with device type distribution (desktop/mobile/tablet)
  - **Locked accounts list**: Shows locked users with unlock button and remaining lockout time
  - **Recent login activity**: Table of last 20 login attempts with success/fail status
  - **Failed login trends**: Hourly trend chart for failed login attempts
  - **Security alerts**: Suspicious patterns detected (multiple failed logins, 2FA failures, password reset spam, lockouts)
  - **Critical events**: List of CRITICAL severity security log entries
  - **Suspicious login activity**: New device/location logins with force logout option
  - **Quick actions**: Unlock account dialog, force logout dialog, event details viewer
  - **Auto-refresh**: Dashboard refreshes every minute
- **Learnings for future iterations**:
  - Security score formula: Start at 100, deduct for failed login ratio (max -30), locked accounts (max -20), critical events (max -30), suspicious logins (max -20)
  - Use Promise.all for parallel fetching of multiple security metrics
  - Hourly trend processing: Create time buckets, count events per bucket
  - Recharts works well for security visualizations (AreaChart for trends, PieChart for device breakdown, BarChart for severity distribution)
  - Suspicion indicators: isNewDevice, isNewLocation from LoginActivity model
  - Force logout = revoke all sessions for a user
  - Session by device uses groupBy on deviceType field
---

## 2026-01-12 - US-051
- **Implemented**: Multiple payment gateways for Cambodian market
- **Files changed**:
  - `lib/payment-gateways.ts` (new file - payment gateway configurations and types)
  - `app/api/payments/gateway/route.ts` (new file - unified payment initialization endpoint)
  - `app/api/payments/verify/route.ts` (new file - payment verification endpoint)
  - `components/checkout-page.tsx` (modified - added payment method selection UI)
  - `components/ui/radio-group.tsx` (new file - Shadcn RadioGroup component)
  - `lib/i18n.ts` (modified - added payment translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **PaymentGatewayType**: CASH, ABA_PAYWAY, WING, KHQR with config fields
  - **Payment gateway configs**: Each gateway has name (EN/KH), description (EN/KH), icon, configFields for admin setup
  - **POST /api/payments/gateway**: Initialize payment with selected gateway
    - CASH: Creates COD order, auto-confirms, returns confirmationRequired
    - ABA_PAYWAY: Generates hash, creates redirectUrl to PayWay checkout
    - WING: Generates signature, creates redirectUrl to Wing checkout
    - KHQR: Generates QR code image via lib/khqr.ts, returns qrImage
  - **GET /api/payments/gateway**: Returns available gateways for checkout
  - **POST /api/payments/verify**: Check payment status (polling for KHQR)
  - **PUT /api/payments/verify**: Admin update payment status manually
  - **Checkout UI**:
    - RadioGroup selection of 4 payment methods with icons
    - Processing state with spinner
    - QR code display screen with 15-minute countdown timer
    - Payment verification button for KHQR
    - Failed/retry screen
    - Success screen with COD-specific instructions
  - **Bilingual translations**: All payment UI text in English and Khmer
- **Learnings for future iterations**:
  - ABA PayWay uses SHA512 hash of concatenated fields
  - Wing uses HMAC-SHA256 signature
  - KHQR QR code display needs countdown timer (15 min expiry)
  - COD flow auto-confirms order, shows "pay on delivery" instructions
  - Payment response types differ per gateway (redirectUrl vs qrImage vs confirmationRequired)
  - RadioGroup from @radix-ui/react-radio-group already in package.json
  - Keep messaging order options (Telegram/Messenger) as fallback
---

## 2026-01-12 - US-052
- **Implemented**: Checkout optimization
- **Files changed**:
  - `prisma/schema.prisma` (modified - added ShippingAddress model)
  - `app/api/shipping-addresses/route.ts` (new file - CRUD endpoint)
  - `components/checkout-page.tsx` (modified - complete rewrite with one-page checkout)
  - `components/shop-app.tsx` (modified - passed onUpdateQuantity/onRemoveItem to checkout)
  - `lib/i18n.ts` (modified - added checkout translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Guest checkout**: Works without account creation (name + phone required)
  - **One-page checkout**: All 3 steps visible: Contact Info, Delivery Address, Payment Method
  - **Shipping address saving**: ShippingAddress model with customerId OR guestId support
  - **Phone-based address lookup**: Enter phone, click "Check" to find saved addresses
  - **Address selection UI**: Saved addresses shown as cards with Home/Work icons, default badge
  - **New address form**: Label, province, district, commune, street, landmark fields
  - **Save for future**: Checkbox to save new addresses for returning customers
  - **Order summary sidebar**: Sticky sidebar with product images, names, prices
  - **Quantity editing**: +/- buttons and remove icon directly in order summary
  - **Express checkout**: Auto-select default address, pre-fill name from saved address
  - **Bilingual**: All UI text in English and Khmer
- **Learnings for future iterations**:
  - Guest checkout works by using guestId (localStorage) instead of customerId for non-logged-in users
  - Phone lookup enables returning customers to find their addresses without full account system
  - One-page checkout uses numbered section headers (1, 2, 3) for visual progress indication
  - Sticky sidebar with overflow scroll works well for order summary
  - Form validation: require fullName + phone + (selected address OR new address line)
  - ShippingAddress stores full Cambodia address hierarchy: province, district, commune, street
---

## 2026-01-12 - US-053
- **Implemented**: Coupon and discount system
- **Files changed**:
  - `prisma/schema.prisma` (modified - added Coupon and CouponUsage models)
  - `app/api/coupons/route.ts` (new file - CRUD endpoint)
  - `app/api/coupons/[id]/route.ts` (new file - single coupon GET/PUT/DELETE)
  - `app/api/coupons/apply/route.ts` (new file - coupon validation and discount calculation)
  - `app/api/coupons/use/route.ts` (new file - record coupon usage)
  - `components/coupon-input.tsx` (new file - UI component)
  - `components/checkout-page.tsx` (modified - integrated coupon input)
  - `lib/i18n.ts` (modified - added coupon translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Coupon model**: code, type (PERCENTAGE/FIXED_AMOUNT/FREE_SHIPPING/BOGO), value, minOrderUsd, maxDiscountUsd, maxUses, maxUsesPerCustomer, startsAt, expiresAt, applicableProductIds, applicableCategoryIds
  - **CouponUsage model**: tracks couponId, customerId, guestId, orderId, discountUsd, discountKhr
  - **CRUD /api/coupons**: List with pagination/search, create with validation, update, soft delete if used
  - **Apply endpoint**: Validates coupon eligibility, calculates discount based on type, supports product/category restrictions
  - **Use endpoint**: Records usage after order, increments usedCount in transaction
  - **CouponInput component**: Expandable input field, applies coupon via API, shows applied state with remove option
  - **Checkout integration**: Shows coupon in order summary, displays discount line, updates final total
  - **Bilingual translations**: All coupon UI text in English and Khmer
- **Learnings for future iterations**:
  - Coupon types require different calculation logic: PERCENTAGE uses maxDiscountUsd cap, BOGO finds cheapest item
  - FREE_SHIPPING type discounts the shipping amount (0 in current implementation, but structure supports it)
  - applicableProductIds/applicableCategoryIds stored as JSON arrays in Prisma for flexible filtering
  - CouponInput uses optimistic UI pattern - shows applied state immediately while recording usage asynchronously
  - Guest checkout uses localStorage guestId for usage tracking
  - Coupon code input auto-converts to uppercase for consistency
---

## 2026-01-12 - US-054
- **Implemented**: Flash sales and time-limited deals
- **Files changed**:
  - `prisma/schema.prisma` (added FlashSale model with FlashSaleStatus enum)
  - `app/api/flash-sales/route.ts` (new - CRUD endpoint with validation)
  - `app/api/flash-sales/product/[id]/route.ts` (new - get active sale for product)
  - `components/flash-sale-countdown.tsx` (new - countdown timer, badge, price components)
  - `components/flash-sale-banner.tsx` (new - homepage banner with carousel)
  - `components/pages/flash-sales-page.tsx` (new - admin page with list/calendar views)
  - `components/admin-dashboard.tsx` (added FlashSalesPage import and case)
  - `components/sidebar.tsx` (added Flash Sales nav item with Zap icon)
  - `components/product-grid.tsx` (integrated flash sale badge, countdown, pricing)
  - `lib/api-hooks.ts` (added FlashSale types and hooks)
  - `lib/i18n.ts` (added flashSale translations EN/KH)
- **Features**:
  - FlashSale model: productId, salePriceUsd/Khr, startTime, endTime, quantity, soldCount, status (SCHEDULED/ACTIVE/ENDED/CANCELLED), nameEn/Kh, descriptionEn/Kh, isFeatured, bannerImageUrl
  - CRUD API with overlap detection, price validation, status auto-calculation
  - FlashSaleCountdown: default/compact/banner variants, auto-refresh, onExpire callback
  - FlashSaleBadge: discount percentage display with gradient background
  - FlashSalePrice: original price strikethrough with sale price highlight
  - FlashSaleBanner: auto-rotating carousel for featured sales with stock progress bar
  - Admin page: list view with table, calendar view with monthly grid, create/edit dialog, stats cards
  - Product grid integration: badge on card, countdown, sale pricing, gradient add-to-cart button
  - useActiveFlashSales hook: batch fetches flash sales for visible products, 1-minute refetch
- **Learnings for future iterations**:
  - FlashSale status determined by startTime/endTime, not manual toggle - status changes automatically
  - Calendar view uses date range spanning (single sale can appear on multiple days)
  - Flash sale price overrides regular price in cart (passed to onAddToCart)
  - Overlap validation prevents multiple active flash sales on same product
  - remainingQuantity = quantity - soldCount (null quantity = unlimited)
  - useActiveFlashSales returns map of productId -> FlashSaleForProduct for O(1) lookup
---

## 2026-01-12 - US-055
- **Implemented**: Customer loyalty program
- **Files changed**:
  - `prisma/schema.prisma` (modified - added LoyaltyAccount, LoyaltyTransaction models, LoyaltyTier and LoyaltyTransactionType enums)
  - `lib/loyalty.ts` (new file - loyalty service with business logic)
  - `app/api/loyalty/route.ts` (new file - GET/POST endpoint)
  - `lib/api-hooks.ts` (modified - added loyalty types and hooks)
  - `components/loyalty-points-card.tsx` (new file - customer loyalty display)
  - `components/checkout-loyalty.tsx` (new file - checkout redemption UI)
  - `lib/i18n.ts` (modified - added loyalty translations EN/KH)
- **Features**:
  - **LoyaltyAccount model**: customerId, currentPoints, lifetimePoints, tier (Bronze/Silver/Gold/Platinum)
  - **LoyaltyTransaction model**: accountId, type (EARN/REDEEM/BONUS/EXPIRE/ADJUSTMENT/REFUND), points, orderId, expiresAt
  - **Tier system**: Bronze (1x), Silver (1.25x, 500+ pts), Gold (1.5x, 2000+ pts), Platinum (2x, 5000+ pts)
  - **Points earning**: 1 point per $1 spent, multiplied by tier multiplier
  - **Points redemption**: 100 points = $1 discount, min 100 points to redeem
  - **Points expiry**: 12 months from earning date, processExpiredPoints() for batch expiry
  - **API actions**: earn, redeem, calculate, pointsNeeded
  - **LoyaltyPointsCard**: Full card with tier badge, progress to next tier, transaction history
  - **LoyaltyPointsBadge**: Mini widget for header/nav display
  - **CheckoutLoyalty**: Slider/input for point redemption, shows points to earn
- **Learnings for future iterations**:
  - Used separate currentPoints (spendable) and lifetimePoints (for tier calculation)
  - Tier upgrades are automatic on earn, but no downgrades on refund (better UX)
  - Points expiry tracked per-transaction with expiresAt field, batch job processes expired
  - Redemption value capped at order total to prevent over-discount
  - Existing Progress component from Shadcn used for tier progress bar
  - Slider component from Shadcn used for point selection in checkout
---

## 2026-01-12 - US-056
- **Implemented**: Email marketing integration
- **Files changed**:
  - `prisma/schema.prisma` (modified - added NewsletterSubscriber, EmailCampaign, EmailSend, AbandonedCart, WelcomeEmailProgress models)
  - `lib/email-marketing.ts` (new file - email marketing service)
  - `components/newsletter-form.tsx` (new file - newsletter subscription form)
  - `app/api/newsletter/subscribe/route.ts` (new file)
  - `app/api/newsletter/unsubscribe/route.ts` (new file)
  - `app/api/email/cron/route.ts` (new file - cron for automated emails)
  - `lib/i18n.ts` (modified - added newsletter translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **NewsletterSubscriber model**: email, status, source, language, unsubscribeToken, confirmedAt
  - **EmailCampaign model**: type (NEWSLETTER/PROMOTIONAL/TRANSACTIONAL/WELCOME/ABANDONED_CART/WINBACK), bilingual content, scheduling, statistics
  - **EmailSend model**: tracks individual sends with status (PENDING/SENT/DELIVERED/OPENED/CLICKED/BOUNCED/FAILED)
  - **AbandonedCart model**: tracks cart contents, recovery token, email sequence status (1hr/24hr/72hr)
  - **WelcomeEmailProgress model**: tracks welcome email series (3 emails)
  - **SendGrid integration**: sendViaSendGrid() with API key support
  - **SMTP fallback**: uses existing notification-service.ts sendEmailNotification()
  - **Newsletter form**: NewsletterForm and NewsletterSection components, stacked/inline variants
  - **Abandoned cart recovery**: processAbandonedCarts() sends 3 emails at 1hr, 24hr, 72hr intervals
  - **Welcome series**: startWelcomeSeries() and processWelcomeSeries() for 3-email onboarding
  - **Order emails**: sendOrderConfirmationEmail() and sendShippingNotificationEmail()
  - **Cron endpoint**: /api/email/cron for scheduled processing (CRON_SECRET auth)
- **Learnings for future iterations**:
  - Email campaign types cover all use cases: NEWSLETTER, PROMOTIONAL, TRANSACTIONAL, WELCOME, ABANDONED_CART, WINBACK
  - Abandoned cart detection: mark as abandoned after 1 hour of inactivity
  - Recovery token pattern: unique token for cart restoration without login
  - Welcome series timing: immediate, +1 day, +3 days with discount code in final email
  - SendGrid returns 202 for success, message ID in x-message-id header
  - Cron job should be called hourly via Vercel Cron or external scheduler
  - Unsubscribe tokens stored in DB, one-click unsubscribe via URL param
---

## 2026-01-12 - US-057
- **Implemented**: SEO optimization
- **Files changed**:
  - `app/sitemap.ts` (new file - dynamic sitemap generation)
  - `components/json-ld.tsx` (new file - JSON-LD structured data components)
  - `components/product-detail-client.tsx` (new file - client-side product detail with SEO integration)
  - `lib/seo.ts` (new file - SEO utilities: slug generation, JSON-LD helpers, metadata helpers)
  - `app/shop/product/[id]/page.tsx` (modified - added generateMetadata, JSON-LD, canonical URLs)
  - `lib/server-utils.ts` (modified - added getProductByIdOrSlug with review stats for SEO)
  - `lib/api-hooks.ts` (modified - type exports for SEO components)
  - `prisma/schema.prisma` (modified - confirmed slug field on Product model)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Dynamic meta tags**: generateMetadata() for product pages with title, description, keywords, Open Graph, Twitter Cards
  - **Sitemap.xml**: Auto-generated at /sitemap.xml with static pages, products (using slug or ID), and categories
  - **JSON-LD structured data**: ProductJsonLd, BreadcrumbJsonLd, OrganizationJsonLd, WebsiteJsonLd, LocalBusinessJsonLd components
  - **Canonical URLs**: alternates.canonical set on all product pages to prevent duplicate content
  - **SEO-friendly slugs**: Product slug field support with ID fallback for backwards compatibility
  - **SEO utilities**: generateSlug(), generateUniqueSlug(), truncateForMeta(), getBaseUrl(), getCanonicalUrl(), getProductUrl(), getCategoryUrl()
  - **Product page SEO**: Full metadata with product name, description, price, category, images
  - **Review integration**: Aggregate rating included in JSON-LD when reviews exist
- **Learnings for future iterations**:
  - Next.js sitemap() function in app router returns MetadataRoute.Sitemap array
  - JSON-LD rendered via dangerouslySetInnerHTML with script type="application/ld+json"
  - Product URL prefers slug over ID for better SEO, with graceful fallback
  - getBaseUrl() checks NEXT_PUBLIC_BASE_URL, then VERCEL_URL, then localhost default
  - Prisma query for sitemap uses select with only needed fields for performance
  - Review stats attached to product via _reviewStats for SSR SEO metadata
  - Sitemap includes changeFrequency and priority for search engine hints
---

## 2026-01-12 - US-058
- **Implemented**: Image optimization pipeline
- **Files changed**:
  - `lib/image-optimizer.ts` (new file - server-side image processing with sharp)
  - `app/api/images/optimize/route.ts` (new file - optimized image upload endpoint)
  - `components/optimized-image.tsx` (new file - lazy loading image component)
  - `package.json` (modified - added sharp and @types/sharp)
- **Features**:
  - **Multi-size generation**: thumbnail (80px), small (320px), medium (640px), large (1024px), xlarge (1920px)
  - **WebP conversion**: Primary format with 82% quality for best compression/quality balance
  - **Fallback formats**: JPEG (85% quality, mozjpeg) for older browsers, PNG for transparency
  - **Blur placeholder (LQIP)**: 20x20 blurred JPEG base64 data URL for instant loading
  - **Smart compression**: Size-appropriate compression without visible quality loss
  - **POST /api/images/optimize**: Accepts file upload, returns optimized variants + blur placeholder
  - **OptimizedImage component**: picture element for WebP fallback, Intersection Observer lazy loading, blur-to-clear transition, responsive srcset
  - **useImageOptimizer hook**: Upload helper with progress tracking
  - **CDN integration**: Cloudflare R2 with 1-year cache headers (existing infrastructure)
- **Learnings for future iterations**:
  - sharp is the gold standard for Node.js image processing, handles all formats
  - WebP provides 25-35% smaller files than JPEG at same quality
  - Blur placeholder (LQIP) should be very small (20x20) - it's only seen for ~100ms
  - Intersection Observer rootMargin of 200px starts loading just before visible
  - picture element source order matters: WebP first, then fallback
  - fetchPriority="high" for above-fold images, "auto" for lazy loaded
  - Native loading="lazy" + IntersectionObserver = best lazy loading support
---

## 2026-01-12 - US-059
- **Implemented**: Performance monitoring and error tracking
- **Files changed**:
  - `prisma/schema.prisma` (modified - added PerformanceMetric and ErrorLog models)
  - `lib/performance.ts` (new file - performance service)
  - `app/api/performance/route.ts` (new file - main API endpoint)
  - `app/api/performance/errors/route.ts` (new file - errors endpoint)
  - `lib/api-hooks.ts` (modified - added performance hooks)
  - `components/web-vitals-reporter.tsx` (new file - client-side Web Vitals)
  - `components/pages/performance-page.tsx` (new file - admin dashboard)
  - `components/admin-dashboard.tsx` (modified - added PerformancePage)
  - `components/sidebar.tsx` (modified - added Performance nav item)
  - `package.json` (modified - added web-vitals dependency)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Core Web Vitals**: LCP, FID, CLS, FCP, TTFB, INP with thresholds and ratings
  - **API response time tracking**: logApiResponseTime with rating based on duration
  - **Database query logging**: logDatabaseQuery with performance ratings
  - **Error tracking**: ErrorLog model with fingerprinting for grouping similar errors
  - **Performance dashboard**: 5 tabs (Overview, Web Vitals, API, Database, Errors)
  - **WebVitalsReporter component**: Client-side metrics collection with batching
  - **Error boundaries**: Global error and unhandledrejection handlers
- **Learnings for future iterations**:
  - web-vitals package provides onCLS, onFCP, onLCP, onTTFB, onINP for easy integration
  - Metrics are batched for 5 seconds before sending to reduce API calls
  - Error fingerprinting normalizes numbers in messages for better grouping
  - Performance thresholds: API <100ms good, <500ms needs work; DB <50ms good, <200ms needs work
  - Use visibilitychange event to flush metrics when user leaves page
---

## 2026-01-12 - US-060
- **Implemented**: Live chat support
- **Files changed**:
  - `prisma/schema.prisma` (modified - added ChatSession, ChatMessage, CannedResponse, AgentStatus models)
  - `app/api/chat/sessions/route.ts` (new file - sessions API)
  - `app/api/chat/messages/route.ts` (new file - messages API)
  - `app/api/chat/agent-status/route.ts` (new file - agent online status API)
  - `app/api/chat/canned-responses/route.ts` (new file - canned responses CRUD)
  - `lib/api-hooks.ts` (modified - added chat hooks)
  - `lib/i18n.ts` (modified - added chat translations EN/KH)
  - `components/chat-widget.tsx` (new file - customer chat widget)
  - `components/pages/chat-page.tsx` (new file - admin chat dashboard)
  - `components/shop-app.tsx` (modified - integrated ChatWidget)
  - `components/admin-dashboard.tsx` (modified - added ChatPage)
  - `components/sidebar.tsx` (modified - added Live Chat nav item)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **ChatSession model**: Tracks customer/guest sessions with status (ACTIVE, WAITING, RESOLVED, OFFLINE)
  - **ChatMessage model**: Stores messages with sender type (CUSTOMER, AGENT, SYSTEM)
  - **CannedResponse model**: Pre-defined responses with shortcuts and categories
  - **AgentStatus model**: Tracks agent online status via heartbeat
  - **Chat widget**: Floating bubble on shop pages, opens chat window with messages
  - **Offline form**: When agents offline, customers can leave name/email/message
  - **Admin dashboard**: View all sessions, join chats, send messages, manage canned responses
  - **Quick responses**: Press "/" in message input to insert canned responses
  - **Bilingual support**: Full EN/KH translations for all chat UI
- **Learnings for future iterations**:
  - Session polling at 3-5 second intervals works well for near-real-time without overwhelming server
  - Agent heartbeat (30s interval) with 2-minute threshold for offline detection
  - Guest ID stored in localStorage for session persistence across page refreshes
  - Canned response shortcuts (e.g., /hello) allow quick insertion of common replies
  - System messages (e.g., "Agent joined the chat") provide context for customer
  - Chat status flow: ACTIVE (chatting) -> WAITING (customer waiting) -> RESOLVED (closed)
  - OFFLINE status is separate for messages submitted when no agents online
---

## 2026-01-12 - US-061
- **Implemented**: Help center and FAQ system
- **Files changed**:
  - `prisma/schema.prisma` (added HelpCategory, HelpArticle models)
  - `app/api/help/categories/route.ts` (new - CRUD API)
  - `app/api/help/articles/route.ts` (new - CRUD API with search)
  - `app/api/help/articles/view/route.ts` (new - view tracking)
  - `app/api/help/articles/feedback/route.ts` (new - helpful/not helpful)
  - `lib/api-hooks.ts` (added help center hooks)
  - `lib/i18n.ts` (added help translations EN/KH)
  - `components/pages/help-center-page.tsx` (new - admin page)
  - `components/help-center.tsx` (new - customer-facing component)
  - `components/admin-dashboard.tsx` (added help-center nav)
  - `components/sidebar.tsx` (added help-center nav item)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **HelpCategory model**: nameEn, nameKh, slug, description, sortOrder, iconName, isActive
  - **HelpArticle model**: titleEn, titleKh, slug, contentEn, contentKh, status, viewCount, helpfulYes/No, isFeatured
  - **Search API**: Full-text search across title and content in both languages
  - **Category filtering**: Articles organized by category
  - **View tracking**: Automatic increment on article view
  - **Feedback system**: Helpful yes/no voting
  - **Admin page**: Tabbed interface for articles and categories management
  - **Customer page**: Searchable FAQ with featured articles, categories, popular articles
  - **Bilingual**: Full EN/KH translation support
- **Learnings for future iterations**:
  - Created both HelpCategory and HelpArticle models for proper organization
  - Added SEO fields (metaTitleEn/Kh, metaDescEn/Kh) for future SEO features
  - Searchable FAQ uses case-insensitive contains on title and content fields
  - View tracking is non-blocking - done via separate POST endpoint
  - Featured articles displayed prominently on help center home
  - Popular articles sorted by viewCount for engagement insights
  - Admin page follows existing pattern from content-page.tsx with dialog modals
---

## 2026-01-12 - US-062
- **Implemented**: Shipping zone configuration with flexible rates by location
- **Files changed**:
  - `prisma/schema.prisma` (added ShippingZone model, ShippingRateType enum)
  - `app/api/shipping-zones/route.ts` (new - CRUD API for zones)
  - `app/api/shipping-zones/calculate/route.ts` (new - shipping cost calculation)
  - `lib/api-hooks.ts` (added shipping zone hooks and types)
  - `lib/cambodia-regions.ts` (new - 25 provinces with districts)
  - `components/pages/shipping-zones-page.tsx` (new - admin page)
  - `components/admin-dashboard.tsx` (added shipping-zones nav)
  - `components/sidebar.tsx` (added shipping-zones nav item with Truck icon)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **ShippingZone model**: nameEn, nameKh, regions (JSON array), rateType, flat/weight-based pricing, free threshold, delivery time
  - **3 rate types**: FLAT_RATE (fixed per order), WEIGHT_BASED (per kg), FREE
  - **Free shipping threshold**: freeThresholdUsd/Khr triggers free shipping over amount
  - **Delivery time estimates**: minDeliveryDays, maxDeliveryDays per zone
  - **Cambodia data**: All 25 provinces with districts, preset groups (Phnom Penh, Central, etc.)
  - **Calculate endpoint**: POST /api/shipping-zones/calculate returns shipping options for region/cart
  - **Admin page**: Province selector with presets, rate type configuration, delivery time settings
- **Learnings for future iterations**:
  - Prisma stores regions as Json - use array filter on application side
  - Cambodia has 25 provinces (khet) including Phnom Penh as capital (reach thani)
  - Shipping zone presets useful for quick setup (Phnom Penh only, Central provinces, All provinces)
  - Calculate endpoint returns multiple options if multiple zones cover a region
  - Weight-based shipping needs baseWeightKg for "first X kg free" pricing
  - sortOrder important for determining which zone shows first to customer
  - Admin page pattern: Dialog with ScrollArea for long province list
---

## 2026-01-13 - US-063
- **Implemented**: Order tracking system with courier integration
- **Files changed**:
  - `prisma/schema.prisma` (added OrderTracking, TrackingStatusHistory models, CourierProvider, TrackingStatus enums)
  - `app/api/tracking/route.ts` (new - CRUD API for tracking)
  - `app/api/tracking/lookup/route.ts` (new - public tracking lookup)
  - `app/shop/track/page.tsx` (new - customer tracking page)
  - `components/pages/order-tracking-page.tsx` (new - admin tracking management)
  - `lib/api-hooks.ts` (added tracking hooks and types)
  - `lib/i18n.ts` (added tracking translations EN/KH)
  - `components/admin-dashboard.tsx` (added order-tracking page)
  - `components/sidebar.tsx` (added order-tracking nav item with MapPin icon)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **OrderTracking model**: orderId, trackingNumber, courier, status, estimatedDeliveryDate, actualDeliveryDate, shippingAddress (JSON), notification preferences
  - **TrackingStatusHistory model**: status changes with location, notes, timestamps
  - **CourierProvider enum**: JT_EXPRESS, NINJA_VAN, WING_DELIVERY, OTHER
  - **TrackingStatus enum**: PENDING, PICKED_UP, IN_TRANSIT, OUT_FOR_DELIVERY, DELIVERED, FAILED_DELIVERY, RETURNED
  - **Customer tracking page** at /shop/track: Search by order number or tracking number, status timeline visualization, bilingual support
  - **Admin tracking page**: Create tracking for orders, update status with location/notes, view status history
  - **Public lookup endpoint**: /api/tracking/lookup returns sanitized public view
  - **Notification settings**: notifyOnStatusChange, notifyViaTelegram, notifyViaSms flags
- **Learnings for future iterations**:
  - OrderTracking has 1:1 relation with Order (unique orderId)
  - Status history allows full tracking lifecycle audit
  - Customer page needs useLanguage from shop-context (not useShopLanguage)
  - Language is "EN" | "KH" uppercase in shop context
  - Couriers in Cambodia: J&T Express and Ninja Van are most popular
  - Public lookup should NOT expose internal IDs or customer details
  - Admin page pattern: separate dialogs for create vs update operations
  - Status icons and colors improve tracking UX
---

## 2026-01-13 - US-064
- **Implemented**: Product variants and options system
- **Files changed**:
  - `prisma/schema.prisma` (modified - added ProductVariant model, hasVariants/variantTypes to Product)
  - `components/variant-selector.tsx` (new file - variant selector UI component)
  - `components/product-detail-client.tsx` (modified - integrated variant selector)
  - `lib/server-utils.ts` (modified - include variants in product fetch)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **ProductVariant model**: productId, sku, options (JSON), priceUsd, priceKhr, stock, imageUrl, sortOrder
  - **Product fields**: hasVariants flag, variantTypes array for configuring variant dimensions
  - **Variant types**: size, color, material, style, pattern, weight, flavor, scent (with EN/KH labels)
  - **Color swatches**: Visual color buttons using hex color map (30+ colors), checkmark on selection
  - **Text buttons**: For non-color variants (size, material, etc.)
  - **Stock per option**: Options show as disabled/unavailable when variant has 0 stock
  - **Variant pricing**: Display price difference from base price
  - **Image switching**: Display variant-specific images when selected
  - **Cart integration**: Cart items include variantId, variantSku, variantOptions
  - **Bilingual**: Full EN/KH support for labels, stock status, prompts
- **Learnings for future iterations**:
  - Variant options stored as JSON object: `{ "size": "M", "color": "Red" }`
  - hasVariants flag differentiates products with/without variants for UI logic
  - variantSelectionRequired blocks add-to-cart until all options selected
  - Color map includes common colors + tailwind palette names
  - Prisma Decimal needs type casting for variant prices (toNumber check)
  - Cart items should use composite key (productId + variantId) to handle same product with different variants
  - VariantSelector component is reusable - takes variants array and variantTypes
  - Server-utils includes variants in product fetch with isActive filter and sortOrder
---

## 2026-01-13 - US-065
- **Implemented**: Barcode and SKU management
- **Files changed**:
  - `lib/sku-barcode.ts` (new) - SKU generation and barcode utilities
  - `components/barcode-scanner.tsx` (new) - Camera-based barcode scanner with manual fallback
  - `components/barcode-label.tsx` (new) - Barcode label printing with multiple sizes
  - `app/api/sku/route.ts` (new) - SKU generation API
  - `app/api/inventory/scan/route.ts` (new) - Scan-to-add inventory API
  - `components/pages/inventory-page.tsx` (modified) - Added scan-to-add and print labels
  - `components/pages/products-page.tsx` (modified) - Added auto-generate SKU and print label buttons
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - Auto-generate SKU: Format {CAT}-{PROD}{SEQ} (e.g., COF-LC001)
  - BarcodeScanner: Uses BarcodeDetector API with camera, manual input fallback
  - BarcodeLabel: Three sizes (38x25mm, 50x30mm, 60x40mm), SVG download, print preview
  - PrintPreviewDialog: Bulk label printing with quantity per product
  - Scan-to-add: Quick inventory updates via barcode scan, add/subtract actions
  - EAN-13 barcode generation from SKU for label printing
  - SKU search already existed in products table filter
- **Learnings for future iterations**:
  - BarcodeDetector API not available in all browsers, needs manual input fallback
  - For camera-based scanning: `navigator.mediaDevices.getUserMedia()` with facingMode: 'environment'
  - EAN-13 check digit calculation: sum of odd positions * 1 + even positions * 3, mod 10
  - Label sizes: Common label paper sizes in mm (38x25, 50x30, 60x40)
  - SKU format: Keep short but descriptive - category code (3 chars) + product initials + sequence
  - Print: Use window.open() with print styles for label printing
---

## 2026-01-13 - US-066
- **Implemented**: Stock alerts and reorder points system
- **Files changed**:
  - `prisma/schema.prisma` (modified) - Added Supplier, StockAlert, PurchaseOrder models; updated Inventory with reorderPoint, reorderQty, supplierId
  - `lib/stock-alert-service.ts` (new) - Sales velocity calculation, alert creation, notification sending, purchase order generation
  - `app/api/stock-alerts/route.ts` (new) - GET alerts, POST check/notify, PATCH update status
  - `app/api/suppliers/route.ts` (new) - Full CRUD for suppliers
  - `app/api/purchase-orders/route.ts` (new) - Create from alert or direct, update/receive stock, delete/cancel
  - `app/api/inventory/route.ts` (modified) - Added reorderPoint, reorderQty, supplierId to schema and includes
  - `lib/api-hooks.ts` (modified) - Added StockAlert, Supplier, PurchaseOrder types and hooks
  - `lib/i18n.ts` (modified) - Added stockAlerts translations (EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Reorder point per product**: Inventory model has reorderPoint field (default 5)
  - **Sales velocity calculation**: Weighted 30-day average (70% recent week, 30% previous 3 weeks)
  - **Suggested reorder quantity**: Based on 37-day target stock (30 days + 7 day safety buffer)
  - **Stock alerts**: LOW_STOCK (at reorder point), OUT_OF_STOCK (zero), REORDER_CREATED types
  - **Alert status tracking**: PENDING -> ACKNOWLEDGED -> RESOLVED workflow
  - **Supplier management**: Name, contact info, lead time days, link to products
  - **Purchase orders**: Auto-generated PO numbers (PO-YYYY-XXXX), expected delivery based on lead time
  - **Receive stock**: Partial or full receipt updates inventory and resolves alerts
  - **Notifications**: Telegram/Email alerts when stock hits reorder point (via existing notification service)
- **Learnings for future iterations**:
  - Sales velocity: Weight recent sales higher (70/30 split) for better demand prediction
  - Reorder quantity: Target days of stock + safety buffer accounts for lead time variability
  - Alert deduplication: Check for existing PENDING alert before creating new one
  - Purchase order workflow: DRAFT -> PENDING -> PARTIAL -> COMPLETED progression
  - Stock receipt: Automatically resolve alerts when stock exceeds reorder point
  - In-memory notification settings: Stored in settings table with key "stockAlertNotifications"
---

## 2026-01-13 - US-067
- **Implemented**: Advanced analytics dashboard with business intelligence
- **Files changed**:
  - `app/api/analytics/advanced/route.ts` (new) - API endpoint for advanced analytics data
  - `components/pages/analytics-page.tsx` (new) - Full analytics dashboard with all visualizations
  - `lib/api-hooks.ts` (modified) - Added useAdvancedAnalytics hook and types
  - `components/admin-dashboard.tsx` (modified) - Added AnalyticsPage to navigation
  - `components/sidebar.tsx` (modified) - Added Analytics nav item with TrendingUp icon
  - `package.json` (modified) - Added html2canvas dependency for PDF export
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Revenue trends with comparison**: Line/area chart showing current vs previous period revenue
  - **Period comparison metrics**: Summary cards showing revenue, orders, AOV with % change vs previous period
  - **Conversion funnel**: Visual funnel showing Orders Created -> Confirmed -> In Preparation -> Completed with drop-off rates
  - **Product performance matrix**: Scatter chart plotting revenue vs margin (bubble size = quantity sold)
  - **Customer cohort analysis**: Retention heatmap showing % of customers returning by signup month
  - **Export to PDF**: Using html2canvas + jsPDF for full report export with pagination
  - **Date range selector**: 7 days, 30 days, 90 days, 1 year options
- **API Features**:
  - Raw SQL queries for efficient daily aggregations with comparison periods
  - Cohort calculation using CTE (Common Table Expressions) for customer first order month
  - Product performance with margin calculation (estimated 60% cost ratio)
  - Order status funnel aggregation
- **Learnings for future iterations**:
  - ComposedChart from recharts combines Line, Area, and Bar in one visualization
  - Cohort retention heatmap: Use background color intensity (rgba) based on retention %
  - Scatter chart: ZAxis controls bubble size, good for 3-dimensional data
  - PDF export: html2canvas captures DOM, jsPDF handles pagination for long content
  - Date comparison: Calculate previous period by subtracting period length from start date
  - Prisma raw SQL: Use $queryRaw with template literals for complex aggregations
---

## 2026-01-13 - US-068
- **Implemented**: Google Analytics 4 integration
- **Files changed**:
  - `lib/ga4.ts` (new file - GA4 tracking utilities)
  - `providers/ga4-provider.tsx` (new file - GA4 provider component)
  - `app/layout.tsx` (modified - added GA4Provider to layout)
  - `components/pages/settings-page.tsx` (modified - added GA4 settings section)
  - `components/shop-app.tsx` (modified - added GA4 tracking events)
  - `components/search-dropdown.tsx` (modified - added search tracking)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **GA4 script injection**: Uses Next.js Script component with afterInteractive strategy
  - **E-commerce events**: view_item, add_to_cart, remove_from_cart, view_cart, begin_checkout, purchase, refund
  - **Enhanced conversions**: setEnhancedConversionData for better attribution with customer data
  - **Custom events**: search, sign_up, login, share, add_to_wishlist, generate_lead
  - **Admin configuration**: Enable/disable toggle, Measurement ID input (G-XXXXXXXXXX format validation), enhanced conversions toggle, debug mode toggle
  - **Utility functions**: cartItemToGA4Item, productToGA4Item for converting data to GA4 format
- **Learnings for future iterations**:
  - GA4 Measurement ID format: G-[10 alphanumeric characters]
  - Use `strategy="afterInteractive"` for non-blocking script loading
  - gtag is accessed via window.gtag after dataLayer initialization
  - isGA4Available() check prevents errors when GA4 is disabled
  - Enhanced conversions require email/phone to be normalized (lowercase, trimmed)
  - Settings stored as nested object: `{ ga4: { enabled, measurementId, enhancedConversions, debugMode } }`
  - useEffect with isCartOpen dependency tracks view_cart only on cart open, not on content changes
---

## 2026-01-13 - US-069
- **Implemented**: Facebook Pixel integration with Conversions API
- **Files changed**:
  - `lib/fb-pixel.ts` (new file - helper functions)
  - `providers/fb-pixel-provider.tsx` (new file - provider component)
  - `app/api/conversions/facebook/route.ts` (new file - server-side Conversions API)
  - `components/pages/settings-page.tsx` (modified - added FB Pixel config UI)
  - `app/layout.tsx` (modified - added FBPixelProvider)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **lib/fb-pixel.ts**: Helper functions for all standard FB events (ViewContent, AddToCart, InitiateCheckout, Purchase, etc.)
  - **FBPixelProvider**: Injects FB Pixel script with admin-configurable Pixel ID
  - **Standard events**: ViewContent, AddToCart, InitiateCheckout, AddPaymentInfo, Purchase, Search
  - **Conversions API**: Server-side event tracking with SHA-256 user data hashing
  - **Admin config**: Enable/disable, Pixel ID, Conversions API toggle, access token, test event code, debug mode
  - **Tracked events list**: Shows in settings UI when enabled
- **Learnings for future iterations**:
  - FB Pixel uses 15-16 digit numeric IDs (validate with /^\d{15,16}$/)
  - Conversions API requires access token generated in Events Manager
  - User data for Conversions API must be SHA-256 hashed on server side
  - FB Pixel script initialization uses specific fbq pattern with queue array
  - Test event codes allow testing without affecting live data
  - Pattern follows GA4 integration: helper lib + provider + settings UI
---

## 2026-01-13 - US-070
- **Implemented**: WCAG accessibility compliance
- **Files changed**:
  - `components/shop-app.tsx` (modified - added skip-to-content link, main landmark with id)
  - `components/header.tsx` (modified - added ARIA labels, roles, focus styles, keyboard nav)
  - `components/product-grid.tsx` (modified - added semantic HTML, ARIA labels, focus styles)
  - `components/search-dropdown.tsx` (modified - added listbox role, ARIA attributes)
  - `app/globals.css` (modified - added focus-visible, reduced motion, high contrast styles)
  - `lib/i18n.ts` (modified - added accessibility translations for EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Skip to main content**: Hidden link visible on focus, bilingual (EN/KH)
  - **ARIA labels and roles**: role="banner" on header, role="search" on search, role="navigation" on categories, role="listbox" on search dropdown, role="option" on results
  - **Semantic HTML**: main landmark with id, section for products, nav for categories, ul/li for product list
  - **Focus visibility**: Global focus-visible styles with ring offset, per-element focus styles
  - **Screen reader support**: Descriptive aria-labels on all interactive elements, aria-hidden on decorative icons, aria-live regions for dynamic content
  - **Keyboard navigation**: Focus ring styles, tabIndex management, Enter/Space key handlers
  - **Color contrast**: High contrast mode media query support
  - **Reduced motion**: prefers-reduced-motion media query disables animations
  - **Bilingual a11y translations**: Added accessibility section in i18n.ts for EN/KH
- **Learnings for future iterations**:
  - WCAG 2.4.1 requires bypass blocks (skip links) for keyboard users
  - Use role="main" with aria-label for main content landmark
  - aria-pressed for toggle buttons (language/currency switches)
  - aria-expanded and aria-controls for expandable search dropdown
  - Product cards use role="article" with full aria-label including name, price, stock
  - Search results use role="listbox" with role="option" children
  - focus-visible pseudo-class targets keyboard focus, not mouse clicks
  - prefers-reduced-motion and prefers-contrast for user preferences
  - Decorative images get aria-hidden="true" and empty alt=""
---

## 2026-01-13 - US-071
- **Implemented**: Multi-language expansion with Thai, Vietnamese, Chinese support
- **Files changed**:
  - `components/language-switcher.tsx` (new file) - LanguageSwitcher dropdown and LanguagePillToggle components
  - `lib/i18n.ts` (modified) - Added Language type expansion, LANGUAGE_CONFIG, Thai/Vietnamese/Chinese translations, browser detection
  - `components/header.tsx` (modified) - Updated to use new Language type, integrated LanguageSwitcher
  - `components/shop-app.tsx` (modified) - Added browser language detection, localStorage persistence, handleLanguageChange
  - `components/search-dropdown.tsx` (modified) - Updated Language type from "EN"|"KH" to new Language
  - `components/shop/store-status.tsx` (modified) - Updated to use new Language type
  - `components/shop/announcement-banner.tsx` (modified) - Updated to use new Language type
  - `components/shop/shop-client-wrapper.tsx` (modified) - Added auto-detection, updated Language type
  - `components/pages/settings-page.tsx` (modified) - Added Language Settings card in admin
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **LanguageSwitcher component**: Dropdown with flags, native names, check mark for current selection
  - **5 supported languages**: English (en), Khmer (kh), Thai (th), Vietnamese (vi), Chinese (zh)
  - **LANGUAGE_CONFIG**: Code, name, nativeName, flag emoji, direction (ltr/rtl), locale
  - **Complete translations**: Thai, Vietnamese, Chinese translations for all major sections (orderStatus, search, accessibility, loyalty, flashSale, chat, help, tracking, stockAlerts)
  - **Browser auto-detection**: detectBrowserLanguage() maps browser language codes to supported languages
  - **Language persistence**: localStorage stores preference with key "apolo_language"
  - **RTL infrastructure**: RTL_LANGUAGES array (empty for now), getDirection() function, document.dir update
  - **Admin settings**: Language Settings card with enabled languages toggle, default language select, auto-detect switch
- **Learnings for future iterations**:
  - Language type changed from uppercase "EN"/"KH" to lowercase "en"/"kh"/"th"/"vi"/"zh" for consistency with ISO codes
  - getTranslation(lang) helper provides type-safe translation access with fallback to English
  - Product names only exist in EN/KH, so other languages fall back to English for product display
  - Branding custom messages (customNoResultsMessage) only support en/kh keys
  - RTL support is infrastructure-only for now (empty RTL_LANGUAGES array), ready for Arabic/Hebrew when needed
  - Admin settings use nested "language" key: { defaultLanguage, enabledLanguages, autoDetect }
---

## 2026-01-13 - US-072
- **Implemented**: POS system integration with Square and Loyverse
- **Files changed**:
  - `prisma/schema.prisma` (added POSProvider, POSSync, POSSale, POSProductMapping, POSCustomerMapping models, POSProviderType, POSSyncStatus, POSSyncDirection enums, updated Client model with posProviders relation)
  - `lib/pos-integration.ts` (new file - POS service with Square and Loyverse API support)
  - `lib/i18n.ts` (added pos translations for EN/KH)
  - `app/api/pos/route.ts` (new file - CRUD for POS providers)
  - `app/api/pos/sync/route.ts` (new file - trigger sync operations)
  - `app/api/pos/mappings/route.ts` (new file - product/customer mappings)
  - `app/api/pos/sales/route.ts` (new file - POS sales analytics)
  - `app/api/pos/webhook/square/route.ts` (new file - Square webhook handler)
  - `app/api/pos/webhook/loyverse/route.ts` (new file - Loyverse webhook handler)
  - `app/api/inventory/route.ts` (modified - added real-time POS inventory sync)
  - `components/pages/pos-integration-page.tsx` (new file - admin POS integration UI)
  - `components/admin-dashboard.tsx` (added POSIntegrationPage import and route)
  - `components/sidebar.tsx` (added pos-integration nav item with Store icon)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Prisma models**: POSProvider (Square/Loyverse credentials, sync settings, webhook), POSSync (sync history with progress), POSSale (imported sales data), POSProductMapping (product ID mapping), POSCustomerMapping (customer ID mapping)
  - **Square integration**: Fetch inventory via catalog/inventory APIs, update inventory counts, fetch orders/sales, fetch customers, HMAC webhook verification
  - **Loyverse integration**: Fetch items with variants, update inventory, fetch receipts, fetch customers, webhook verification
  - **Inventory sync**: Real-time bidirectional sync - updates to ApoloShop inventory push to POS, webhook updates from POS update ApoloShop inventory
  - **Sales import**: Import transactions with items, customer mapping, payment method tracking
  - **Customer unification**: Sync customers by phone match or create new, maintain mapping table
  - **Admin UI**: Provider cards with connection status, sync triggers (inventory/sales/customers/full), webhook URL copy, sync history table, settings view
  - **Bilingual support**: EN/KH translations for all POS UI text
- **Learnings for future iterations**:
  - Square API uses OAuth tokens and requires location_id for inventory operations
  - Loyverse API uses access tokens and store_id for location-specific data
  - Webhook verification uses HMAC signatures - Square uses sha256 with base64, Loyverse uses sha256 with hex
  - Real-time sync uses fire-and-forget pattern to avoid blocking API responses
  - Product mappings allow selective sync per product (syncInventory, syncPrice flags)
  - Sales data is denormalized with items JSON for quick analytics queries
---

## 2026-01-13 - US-073
- **Implemented**: Accounting software export functionality
- **Files changed**:
  - `lib/accounting-export.ts` (new file - export utilities)
  - `app/api/accounting/export/route.ts` (new file - API endpoint)
  - `components/pages/accounting-page.tsx` (new file - admin UI)
  - `components/sidebar.tsx` (modified - added accounting nav item)
  - `components/admin-dashboard.tsx` (modified - added accounting page)
  - `lib/i18n.ts` (modified - added accounting translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **QuickBooks IIF Export**: Generate IIF format for direct QuickBooks import with sales receipts and line items
  - **Excel Export**: Multi-sheet workbook with Sales, Daily Summary, and Monthly Summary sheets
  - **CSV Export**: Plain CSV format for local accountants
  - **Tax Report**: Cambodia 10% VAT breakdown by category with taxable/exempt sales
  - **Revenue Summaries**: Daily/monthly summaries with payment and channel breakdowns
  - **Invoice HTML Generation**: Professional invoice layout with seller/buyer info, items table, totals, payment status
  - **Admin UI**: Quick stats (today/month sales, orders, VAT), date range picker, export cards, invoice table, tax tab
  - **Bilingual**: Full EN/KH translations for all UI elements
- **API Endpoint**: GET /api/accounting/export
  - `?format=quickbooks` - QuickBooks IIF file
  - `?format=excel` - Multi-sheet Excel file
  - `?format=csv` - Plain CSV
  - `?format=tax` - Tax report Excel
  - `?format=summary&period=daily|monthly` - Revenue summary JSON
  - `?format=invoice&orderId=xxx` - Invoice HTML for single order
- **Learnings for future iterations**:
  - QuickBooks IIF format uses tab-separated values with !TRNS/!SPL/!ENDTRNS structure
  - XLSX library supports multiple sheets via `XLSX.utils.book_append_sheet()`
  - Cambodia VAT is 10% - calculate as `total / 1.1` for taxable amount
  - Invoice HTML can be opened in browser and printed to PDF natively
  - useSalesReport and useOrders hooks from api-hooks.ts provide data for the UI
---

## 2026-01-13 - US-074
- **Implemented**: Webhook system for event-driven integrations
- **Files changed**:
  - `prisma/schema.prisma` (modified - added Webhook, WebhookDeliveryLog models, WebhookEvent/WebhookDeliveryStatus enums)
  - `lib/webhook-service.ts` (new file - webhook delivery with HMAC signatures)
  - `app/api/webhooks/route.ts` (new file - GET/POST/PUT/DELETE endpoints)
  - `app/api/webhooks/[id]/route.ts` (new file - GET details, POST test)
  - `app/api/webhooks/[id]/retry/route.ts` (new file - POST retry failed delivery)
  - `components/pages/webhooks-page.tsx` (new file - admin UI)
  - `components/admin-dashboard.tsx` (modified - added webhooks page)
  - `components/sidebar.tsx` (modified - added webhooks nav item with Webhook icon)
  - `lib/api-hooks.ts` (modified - added webhook hooks and types)
  - `lib/i18n.ts` (modified - added webhooks translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Webhook Model**: id, name, url, events[], secret, isActive, headers (custom), stats (success/failure counts)
  - **WebhookDeliveryLog Model**: Tracks each delivery attempt with status, httpStatus, response, errorMessage, timing
  - **Events Supported**: ORDER_CREATED, ORDER_UPDATED, PRODUCT_UPDATED, CUSTOMER_CREATED
  - **HMAC-SHA256 Signatures**: `X-Webhook-Signature` header with `sha256=...` format for payload verification
  - **Retry Mechanism**: 3 attempts with exponential backoff (1min, 5min, 15min delays)
  - **Admin UI**: Create/edit/delete webhooks, toggle active status, test button, view delivery logs
  - **Secret Management**: Auto-generated 64-char hex secret, masked in API responses, full secret shown only on create
  - **Bilingual**: Full EN/KH translations for UI and delivery status
- **API Endpoints**:
  - `GET /api/webhooks` - List webhooks with pagination
  - `POST /api/webhooks` - Create webhook (returns full secret once)
  - `PUT /api/webhooks` - Update webhook (optional regenerateSecret flag)
  - `DELETE /api/webhooks?id=xxx` - Delete webhook
  - `GET /api/webhooks/[id]` - Get webhook details with stats and delivery logs
  - `POST /api/webhooks/[id]` - Send test webhook
  - `POST /api/webhooks/[id]/retry` - Retry failed delivery
- **Helper Functions**:
  - `triggerWebhooks(event, data, clientId?)` - Trigger all matching webhooks
  - `onOrderCreated(order)` / `onOrderUpdated(order)` - Event-specific helpers
  - `onProductUpdated(product)` / `onCustomerCreated(customer)` - Event-specific helpers
- **Learnings for future iterations**:
  - HMAC signature uses `createHmac('sha256', secret).update(payload).digest('hex')`
  - Timing-safe comparison needed for signature verification to prevent timing attacks
  - Webhook secrets should be 32+ bytes (64 hex chars) for security
  - Fire-and-forget pattern: don't await webhook delivery in main request flow
  - Use AbortController with setTimeout for request timeouts
  - Truncate response body to prevent huge payloads in logs (1000 char limit)
  - Custom headers stored as JSON allow flexible integration with various services
---

## 2026-01-13 - US-075
- **Implemented**: Product bundling with "Frequently bought together" suggestions
- **Files changed**:
  - `prisma/schema.prisma` (modified - added Bundle, BundleItem models, BundleDiscountType enum)
  - `app/api/bundles/route.ts` (new file - CRUD endpoint)
  - `app/api/bundles/suggestions/route.ts` (new file - "frequently bought together" API)
  - `components/product-bundle.tsx` (new file - ProductBundleCard, FrequentlyBoughtTogether, BundleList)
  - `lib/api-hooks.ts` (modified - added bundle hooks and types)
  - `lib/i18n.ts` (modified - added bundle translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - Bundle model with PERCENTAGE and FIXED discount types
  - BundleItem model for product-bundle relationships with quantity and sort order
  - Client relation for multi-tenant support
  - API calculates bundle pricing dynamically (original, discounted, savings)
  - Stock tracking across all bundle items
  - /api/bundles/suggestions analyzes order history for "frequently bought together" products
  - Falls back to same-category products when no order history exists
  - ProductBundleCard shows all bundle items with pricing and savings badge
  - FrequentlyBoughtTogether component for product detail pages with selectable items
  - React Query hooks: useBundles, useBundle, useBundleSuggestions, useCreateBundle, useUpdateBundle, useDeleteBundle
- **Learnings for future iterations**:
  - Bundle pricing is calculated on-the-fly from product prices, not stored
  - isInStock checks all bundle items have sufficient inventory
  - Co-occurrence analysis uses OrderItem data to find frequently bought together products
  - Suggested bundle discount is 10% by default
  - BundleItem has unique constraint on [bundleId, productId] to prevent duplicates
---

## 2026-01-13 - US-076
- **Implemented**: Digital gift card purchase and redemption system
- **Files changed**:
  - `prisma/schema.prisma` (modified - added GiftCard, GiftCardTransaction models, GiftCardStatus enum)
  - `app/api/gift-cards/route.ts` (new file - purchase and check balance endpoints)
  - `app/api/gift-cards/redeem/route.ts` (new file - redeem at checkout endpoint)
  - `app/shop/gift-card/page.tsx` (new file - customer balance check page)
  - `lib/i18n.ts` (modified - added giftCard translations for EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **GiftCard model**: code (XXXX-XXXX-XXXX-XXXX format), initialBalance, currentBalance, currency, status, purchaserEmail, recipientEmail, recipientName, personalMessage, expiresAt
  - **GiftCardTransaction model**: tracks all transactions (purchase, redemption, refund, admin_adjustment)
  - **GiftCardStatus enum**: ACTIVE, REDEEMED, EXPIRED, CANCELLED
  - **Purchase API**: POST /api/gift-cards with amount validation ($5-$500), email validation, unique code generation
  - **Check Balance API**: GET /api/gift-cards?code=&checkBalance=true
  - **Redeem API**: POST /api/gift-cards/redeem with balance validation, partial redemption support
  - **Validate API**: GET /api/gift-cards/redeem?code= for pre-checkout validation
  - **Customer Page**: /shop/gift-card with code input formatting, balance display, status badges
  - **Bilingual**: Full EN/KH translations for all gift card UI elements
- **Learnings for future iterations**:
  - Gift card codes use excludable characters (no I, O, 0, 1 to avoid confusion)
  - Code generation includes retry logic (max 10 attempts) to ensure uniqueness
  - Transactions record balanceAfter for audit trail
  - Partial redemption supported: use min(requestedAmount, currentBalance)
  - Status automatically changes to REDEEMED when balance reaches 0
  - Input formatting in UI auto-adds dashes for better UX (XXXX-XXXX-XXXX-XXXX)
---

## 2026-01-13 - US-077
- **Implemented**: Abandoned cart recovery with discount support and admin dashboard
- **Files changed**:
  - `app/api/abandoned-carts/route.ts` (new file - GET list/analytics, POST actions)
  - `lib/abandoned-cart-service.ts` (new file - enhanced recovery with discounts)
  - `components/pages/abandoned-carts-page.tsx` (new file - admin dashboard)
  - `components/sidebar.tsx` (modified - added abandoned-carts nav item)
  - `components/admin-dashboard.tsx` (modified - added AbandonedCartsPage case)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Existing infrastructure**: AbandonedCart model and processAbandonedCarts() from US-056 already tracked carts
  - **API endpoint**: GET /api/abandoned-carts with pagination, status filter, and rich analytics
  - **Analytics include**: recoveryRate, totalRecovered, recoveredValue, potentialRevenueLost, emailFunnel, dailyTrend, statusBreakdown
  - **Discount support**: Email 1 (no discount), Email 2 (5% off), Email 3 (10% off)
  - **Auto-generated coupons**: RECOVER-{cartId} codes with expiry, max 1 use
  - **Admin actions**: send_recovery_email (with optional discount%), mark_recovered, mark_unrecoverable
  - **Dashboard tabs**: Cart List (table with filters), Analytics (charts), Email Funnel (visualization)
  - **Charts**: Daily trend (AreaChart), Status breakdown (PieChart), Value by status (BarChart), Funnel (custom)
- **Learnings for future iterations**:
  - US-056 already implemented core abandoned cart tracking, this story enhanced with discounts and dashboard
  - sendRecoveryEmailWithDiscount() wraps sendMarketingEmail() with discount HTML and coupon creation
  - Coupon model already existed from US-053, reused via prisma.coupon.upsert()
  - Status flow: ACTIVE -> ABANDONED -> EMAIL_1_SENT -> EMAIL_2_SENT -> EMAIL_3_SENT -> RECOVERED/UNRECOVERABLE
  - Recovery rate = totalRecovered / totalAbandoned * 100
  - Sidebar NavItem type must be updated in both sidebar.tsx and admin-dashboard.tsx
---

## 2026-01-13 - US-078
- **Implemented**: Customer segmentation with RFM analysis
- **Files changed**:
  - `prisma/schema.prisma` (modified - added CustomerSegment, CustomerSegmentMembership models, SegmentType enum)
  - `app/api/segments/route.ts` (new file - CRUD + RFM calculation endpoints)
  - `components/pages/customer-segments-page.tsx` (new file - admin dashboard)
  - `components/sidebar.tsx` (modified - added customer-segments nav item)
  - `components/admin-dashboard.tsx` (modified - added CustomerSegmentsPage case)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **CustomerSegment model**: name, nameKh, description, type (AUTO/CUSTOM), rfmConfig, filterRules, color, icon, customerCount, totalRevenue
  - **CustomerSegmentMembership model**: Links customers to segments with RFM scores (recency, frequency, monetary), lastOrderDays, totalOrders, totalSpent
  - **Auto-segments**: VIP, Active, New, At Risk, Churned, Inactive - automatically assigned based on RFM scores
  - **RFM scoring (1-5 scale)**: Recency (days since last order), Frequency (order count), Monetary (total spend in USD)
  - **API actions**: seed_default_segments, calculate_rfm (single or all customers), assign_customer, remove_customer
  - **Trends endpoint**: GET /api/segments?action=trends&days=30 for segment size over time
  - **Dashboard features**: Stats cards (segments, customers, coverage), segment list table, distribution pie chart, revenue by segment bar chart, RFM scoring guide
  - **Segment CRUD**: Create/edit/delete segments with color picker, type selection, bilingual names
  - **Member details**: View segment members with their individual RFM scores
- **Learnings for future iterations**:
  - RFM quintile thresholds: R(7/30/60/90 days), F(10/5/3/2 orders), M($500/200/100/50)
  - Segment assignment via upsert to handle re-calculation without duplicates
  - Use Prisma.DbNull for clearing optional JSON fields
  - Customer relation added: segmentMemberships CustomerSegmentMembership[]
  - Icons stored as string names (crown, activity, user-plus, etc.) and mapped to Lucide components
---

## 2026-01-13 - US-079
- **Implemented**: A/B testing framework for conversion optimization
- **Files changed**:
  - `prisma/schema.prisma` (modified - added Experiment, ExperimentVariant, ExperimentAssignment models, ExperimentStatus and ExperimentType enums)
  - `app/api/experiments/route.ts` (new file - CRUD endpoints for experiments)
  - `app/api/experiments/assign/route.ts` (new file - visitor variant assignment)
  - `app/api/experiments/convert/route.ts` (new file - conversion tracking)
  - `app/api/experiments/stats/route.ts` (new file - detailed statistics)
  - `lib/ab-testing.ts` (new file - statistical significance calculations)
  - `lib/api-hooks.ts` (modified - added experiment hooks)
  - `components/pages/ab-testing-page.tsx` (new file - admin dashboard)
  - `components/sidebar.tsx` (modified - added ab-testing nav item)
  - `components/admin-dashboard.tsx` (modified - added ABTestingPage case)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Experiment model**: name, description, type, status (DRAFT/RUNNING/PAUSED/COMPLETED), trafficPercent, goalType, confidenceLevel, minSampleSize, autoEndEnabled, timing fields
  - **ExperimentVariant model**: name, isControl, trafficWeight, config (JSON), visitors, conversions, revenue, conversionRate, avgOrderValue
  - **ExperimentAssignment model**: Links visitors to variants, tracks conversions with value and orderId
  - **Traffic splitting**: Weighted random assignment using deterministic hash for consistent visitor experience
  - **Statistical significance**: Z-test for proportions with p-value, confidence, relative improvement calculations
  - **Auto-end**: Automatically completes experiment when statistical significance is reached at target confidence level
  - **API endpoints**: GET/POST/PUT/DELETE experiments, POST /assign for variant assignment, POST /convert for conversion tracking, GET /stats for detailed statistics
  - **Dashboard features**: Create experiment wizard with variant configuration, experiment list table, statistics dialog with summary cards, progress bars, and variant comparison table
  - **Experiment types**: PRODUCT_PAGE, CHECKOUT, PRICING, HOMEPAGE, PROMOTION, CUSTOM
  - **Goal types**: conversion, revenue, clicks, add_to_cart
  - **Confidence levels**: 90%, 95%, 99%
- **Learnings for future iterations**:
  - Deterministic hash (simpleHash) ensures visitors always see same variant across sessions
  - Traffic weights must sum to 100 - validation at creation
  - Exactly one variant must be marked as control
  - Z-test uses pooled proportion for standard error calculation
  - Critical z-values: 90%=1.645, 95%=1.96, 99%=2.576
  - Use Prisma.Decimal for monetary values in conversion tracking
  - Running experiments restrict changes to goalType, confidenceLevel, minSampleSize
  - Soft delete for experiments with data (mark as COMPLETED instead)
  - FlaskConical icon from lucide-react for A/B testing sidebar item
---

## 2026-01-13 - US-080
- **Implemented**: Store locator for multi-location businesses
- **Files changed**:
  - `prisma/schema.prisma` (modified - added StoreLocation model)
  - `app/api/stores/route.ts` (new file - CRUD API with distance calculation)
  - `components/store-locator.tsx` (new file - StoreLocator component)
  - `lib/i18n.ts` (modified - added storeLocator translations for EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **StoreLocation model**: name, nameKh, address, addressKh, lat, lng, phone, email, hours (JSON), hoursNote, description, imageUrl, isActive, isPrimary, sortOrder
  - **API endpoint** `/api/stores`: GET (list with optional ?lat=&lng= for distance), POST, PUT, DELETE
  - **Haversine distance calculation**: Calculates distance between customer and stores in km
  - **Nearest store detection**: API returns `nearest` store when customer coordinates provided
  - **StoreLocator component**: Map view placeholder, store cards with click selection
  - **Open/closed status**: Real-time calculation based on current day/time and store hours
  - **Contact actions**: Click-to-call (tel:), email (mailto:), Google Maps directions links
  - **Bilingual support**: All text uses translations from lib/i18n.ts (EN/KH)
  - **Geolocation**: Uses browser navigator.geolocation API to find user location
- **Learnings for future iterations**:
  - Prisma Decimal types need conversion to Number for JSON serialization
  - Haversine formula uses Earth radius 6371km for distance calculation
  - Operating hours stored as JSON object keyed by lowercase day names
  - Google Maps embed requires API key; fallback to direct link when not configured
  - Store hours check needs to handle timezone differences (currently uses browser local time)
---

## 2026-01-13 - US-081
- **Implemented**: Referral program with unique codes and rewards
- **Files changed**:
  - `prisma/schema.prisma` (modified - added Referral, ReferralCode models, ReferralStatus enum)
  - `lib/referral.ts` (new file - referral service with code generation and rewards)
  - `app/api/referrals/route.ts` (new file - GET/POST endpoint for referral operations)
  - `lib/api-hooks.ts` (modified - added referral hooks and types)
  - `components/referral-dashboard.tsx` (new file - ReferralDashboard component)
  - `lib/i18n.ts` (modified - added referral translations for EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Referral model**: referrerId, refereeId, code, status (PENDING/COMPLETED/EXPIRED/CANCELLED), referrerReward, refereeReward, rewardType, orderId, timestamps
  - **ReferralCode model**: Per-customer unique code, timesUsed, totalEarned tracking
  - **Code generation**: FRIEND-XXXXXX format with alphanumeric characters (avoids confusing 0/O/I/l)
  - **Rewards**: $5 to referrer when friend completes first order, $5 discount to referee
  - **Validation**: Prevents self-referral, existing customer referral, expired codes, monthly limits
  - **API endpoint** `/api/referrals`: GET (code info, stats, validate), POST (apply, complete, updateReferee)
  - **ReferralDashboard component**: Stats grid, share buttons (Telegram, Facebook, copy link), referral history
  - **Share links**: Pre-formatted messages with referral code for Telegram and Facebook sharing
  - **React Query hooks**: useReferralInfo, useValidateReferralCode, useApplyReferral, useCompleteReferral
  - **Bilingual support**: Full EN/KH translations for all UI text and status labels
- **Learnings for future iterations**:
  - Use named Prisma relations for multiple references to same model (@relation("ReferrerCustomer"), @relation("RefereeCustomer"))
  - Referral expiration (30 days) uses expiresAt field set at creation
  - Monthly referral limit (50) prevents abuse while allowing generous sharing
  - generateRandomCode uses chars="ABCDEFGHJKLMNPQRSTUVWXYZ23456789" to avoid confusing characters
  - Clipboard API (navigator.clipboard.writeText) requires async/await and try/catch for browser compatibility
  - Share links encode URLs and messages properly with encodeURIComponent
---

## 2026-01-13 - US-082
- **Implemented**: Social proof widgets for increased trust and conversions
- **Files changed**:
  - `prisma/schema.prisma` (added SocialProofConfig model)
  - `app/api/social-proof/route.ts` (new file - API endpoint)
  - `components/social-proof.tsx` (new file - all social proof components)
  - `lib/i18n.ts` (added socialProof translations for EN/KH)
  - `components/shop-app.tsx` (integrated SocialProofNotification)
  - `components/checkout-page.tsx` (integrated TrustStrip)
  - `components/product-detail-client.tsx` (integrated ProductSocialProof)
- **Features**:
  - **SocialProofConfig model**: Configurable settings for all social proof features (display timing, viewer count ranges, enabled badges, notification position)
  - **Recent purchase notifications**: Popup component showing "X purchased Y" with product image, cycling through recent orders with configurable delay and duration
  - **Currently viewing count**: Shows simulated viewer count on product pages (configurable min/max range)
  - **Items sold counter**: Displays total sold count for products on detail pages
  - **Trust badges**: TrustBadges component with secure checkout, money-back guarantee, fast delivery icons
  - **TrustStrip**: Compact trust strip for checkout page showing secure payment, fast delivery, quality guarantee
  - **API endpoint**: GET /api/social-proof returns config, recent purchases, product stats; POST updates config
  - **Privacy**: Customer names anonymized ("John D." format) in notifications
  - **Bilingual**: Full EN/KH translation support
- **Learnings for future iterations**:
  - Social proof notifications need careful positioning to avoid interfering with other fixed elements
  - Viewer count simulation should use configurable ranges to feel realistic
  - Customer name anonymization is important for privacy compliance
  - Trust badges work best when positioned near payment/checkout actions
  - API should return both config and data in single call for efficiency
  - Non-blocking pattern: notifications can be dismissed without affecting other functionality
---

## 2026-01-13 - US-083
- **Implemented**: Returns and refunds system with customer self-service and admin workflow
- **Files changed**:
  - `prisma/schema.prisma` (added Return model, ReturnStatus/ReturnReason/RefundMethod enums)
  - `app/api/returns/route.ts` (new file - GET list, POST create, PATCH update)
  - `app/api/returns/[id]/route.ts` (new file - GET single, PATCH update, DELETE cancel)
  - `app/api/returns/[id]/refund/route.ts` (new file - POST refund processing)
  - `components/return-request-form.tsx` (new file - customer return request form)
  - `components/pages/returns-page.tsx` (new file - admin returns management dashboard)
  - `components/sidebar.tsx` (added returns nav item with RotateCcw icon)
  - `components/admin-dashboard.tsx` (added ReturnsPage import and case)
  - `lib/i18n.ts` (added returns translations for EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Return model**: returnNumber (RET-YYYY-NNNN), orderId, customerId, status, reason, reasonDetails, items (JSON), refundAmount (USD/KHR), refundMethod, trackingNumber, adminNotes, timestamps
  - **ReturnStatus enum**: PENDING, APPROVED, RECEIVED, REFUNDED, REJECTED, CANCELLED
  - **ReturnReason enum**: DEFECTIVE, WRONG_ITEM, NOT_AS_DESCRIBED, CHANGED_MIND, SIZE_FIT, QUALITY, LATE_DELIVERY, OTHER
  - **RefundMethod enum**: ORIGINAL_PAYMENT, STORE_CREDIT, BANK_TRANSFER
  - **Customer return form**: Select items with quantity, choose reason, add details, see calculated refund amount
  - **Admin workflow**: List view with status filter, approve/reject dialogs, mark received, process refund
  - **Refund processing**: Original payment (creates negative payment record), store credit (adds loyalty points), bank transfer (manual)
  - **Validation**: Only COMPLETED orders eligible, prevents duplicate active returns, verifies items belong to order
  - **API endpoints**: GET/POST /api/returns, GET/PATCH/DELETE /api/returns/[id], POST /api/returns/[id]/refund
  - **Bilingual support**: Full EN/KH translations for all UI text, reasons, and statuses
- **Learnings for future iterations**:
  - Return items stored as JSON array to support partial returns (subset of order items)
  - Refund amount calculated from selected items * quantities, can be overridden by admin
  - Store credit refund uses existing LoyaltyAccount/LoyaltyTransaction models ($1 = 100 points)
  - Original payment refund creates Payment record with negative amount for audit trail
  - Status timestamps (approvedAt, receivedAt, refundedAt, rejectedAt) track workflow progression
  - Admin notes and rejection reason are separate fields for clear communication
  - Return eligibility check: order.status === "COMPLETED" - can be extended with date limits
---

## 2026-01-13 - US-084
- **Implemented**: Tax calculation engine with location-based rates
- **Files changed**:
  - `prisma/schema.prisma` (added TaxRate, TaxLog models, TaxType, TaxPricingMode enums)
  - `lib/tax-service.ts` (new file - tax calculation service)
  - `app/api/tax-rates/route.ts` (new file - GET/POST/PUT/DELETE tax rates)
  - `app/api/reports/tax/route.ts` (new file - tax reporting with CSV export)
  - `lib/api-hooks.ts` (added TaxRate types and hooks: useTaxRates, useCreateTaxRate, useUpdateTaxRate, useDeleteTaxRate, useTaxReport)
  - `components/pages/tax-rates-page.tsx` (new file - admin tax management UI)
  - `components/sidebar.tsx` (added tax-rates nav item with Receipt icon)
  - `components/admin-dashboard.tsx` (added TaxRatesPage import and case)
  - `lib/i18n.ts` (added tax translations for EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **TaxRate model**: country, countryName, region, regionName, name, rate (decimal 0.10 = 10%), taxType, categoryId, pricingMode, isDefault, isActive, effectiveFrom/To, description
  - **TaxLog model**: Audit trail for tax applied to orders - orderId, taxRateId, taxName, taxRateValue, taxType, pricingMode, country, region, subtotal/taxAmount (USD/KHR)
  - **TaxType enum**: VAT, SALES_TAX, GST, CUSTOM
  - **TaxPricingMode enum**: EXCLUSIVE (tax added on top), INCLUSIVE (tax included in price)
  - **Tax calculation service**: getTaxRate() with priority lookup (category-specific > region > country > default), calculateTax() with exclusive/inclusive math, calculateTaxBreakdown() for multi-category orders
  - **Default rates**: Cambodia VAT 10%, Thailand 7%, Vietnam 10%, Singapore GST 9%, Malaysia SST 6%
  - **Admin UI**: Tax Rates tab with CRUD operations, Tax Report tab with date range filter, summary cards, by-country/by-type breakdowns
  - **CSV export**: Download tax logs for accounting with all relevant fields
  - **API endpoints**: GET/POST/PUT/DELETE /api/tax-rates, GET /api/reports/tax (with ?format=csv option)
  - **Bilingual support**: Full EN/KH translations for all tax UI text
- **Learnings for future iterations**:
  - Tax rate lookup uses priority: category-specific rate > region+country > country default > fallback defaults
  - TaxPricingMode determines calculation: EXCLUSIVE adds tax, INCLUSIVE extracts tax from price
  - Prisma Decimal needs Number() conversion for arithmetic operations
  - TaxLog stores snapshot of tax details at time of order for audit compliance (even if rate changes later)
  - Unique constraint on [country, region, categoryId] prevents duplicate rates
  - effectiveFrom/effectiveTo enable scheduled rate changes (e.g., VAT increase from future date)
  - Exchange rate constant (4000 KHR/USD) used for KHR amounts
---

## 2026-01-13 - US-085
- **Implemented**: Multi-currency checkout with auto-detection and conversion fees
- **Files changed**:
  - `prisma/schema.prisma` (added ExchangeRate, CurrencyConversionLog, CurrencyConfig models; expanded Currency enum)
  - `lib/currency-service.ts` (new file - currency management service)
  - `app/api/currency/route.ts` (new file - currency API endpoint)
  - `components/currency-selector.tsx` (new file - CurrencySelector component and useCurrencyDetection hook)
  - `components/header.tsx` (modified - integrated CurrencySelector with multi-currency support)
  - `components/shop-app.tsx` (modified - added currency detection, persistence, and enabled currencies)
  - `components/checkout-page.tsx` (modified - added conversion fee display and settlement currency info)
  - `lib/i18n.ts` (added currency translations for EN/KH)
- **Features**:
  - **ExchangeRate model**: baseCurrency, targetCurrency, rate, inverseRate, conversionFee, source, fetchedAt, expiresAt
  - **CurrencyConversionLog model**: Audit trail for currency conversions with order/customer info
  - **CurrencyConfig model**: Per-shop currency settings (base currency, enabled currencies, fee settings, rate provider)
  - **Currency enum expansion**: Added THB (Thai Baht), VND (Vietnamese Dong), SGD (Singapore Dollar), MYR (Malaysian Ringgit), EUR (Euro), GBP (British Pound)
  - **Currency service**: CURRENCY_INFO with symbols/names/decimals, COUNTRY_CURRENCY_MAP for auto-detection, default exchange rates, convertCurrency(), formatPrice()
  - **Auto-detection**: Browser timezone-based detection, Cloudflare/Vercel IP country headers, fallback to default
  - **API endpoints**: GET /api/currency?action=rates|detect|convert|rate|config, POST for save_config, update_rates, set_rate
  - **CurrencySelector component**: Three variants (pill, compact, full) with exchange rate display and detection badge
  - **useCurrencyDetection hook**: Auto-detects currency on page load, respects stored preference
  - **Checkout integration**: Shows conversion fee percentage, exchange rate info, settlement amount in base currency
  - **Persistence**: Currency preference saved to localStorage, synced across sessions
  - **Bilingual support**: Full EN/KH translations for currency UI text
- **Learnings for future iterations**:
  - Currency detection uses priority: explicit param > CF/Vercel headers > timezone > default
  - Exchange rates stored with inverseRate for bidirectional conversion efficiency
  - CurrencyConfig allows per-shop customization while maintaining system defaults
  - Conversion fees calculated as percentage of total, displayed separately from exchange rate
  - Settlement currency (base) is what shop receives; display currency is what customer sees
  - useCurrencyDetection only auto-sets currency if no stored preference exists
---

## 2026-01-13 - US-086
- **Implemented**: Subscription products (Subscribe & Save)
- **Files changed**:
  - `prisma/schema.prisma` (modified - added ProductSubscription, SubscriptionDelivery models, SubscriptionFrequency, ProductSubscriptionStatus enums)
  - `app/api/subscriptions/route.ts` (new file - GET/POST/PATCH/DELETE endpoints)
  - `components/subscribe-save.tsx` (new file - SubscribeSave UI component)
  - `lib/i18n.ts` (modified - added productSubscription translations EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - ProductSubscription model: customerId, productId, variantId, frequency, quantity, discountPercent, nextDeliveryAt, status
  - SubscriptionDelivery model: tracks each delivery/order from subscription
  - Flexible frequency: WEEKLY, BIWEEKLY, MONTHLY, BIMONTHLY, QUARTERLY
  - Default 10% Subscribe & Save discount
  - PATCH actions: pause, resume, skip, update frequency/quantity
  - DELETE soft-cancels with reason
  - SubscribeSave component with expandable UI, frequency selector, pricing display
- **Learnings for future iterations**:
  - Named model `ProductSubscription` to avoid conflict with existing `Subscription` model (client billing)
  - Used `productSubscription` key in i18n to distinguish from `subscription` (billing)
  - calculateNextDeliveryDate helper handles all frequency types
  - Subscription delivery tracking allows for order history and skip records
  - Build passes without database sync (Prisma schema validates, runtime DB sync separate)
---

## 2026-01-13 - US-087
- **Implemented**: Pre-order functionality for upcoming products
- **Files changed**:
  - `prisma/schema.prisma` (modified - added pre-order fields to Product model)
  - `lib/api-hooks.ts` (modified - added pre-order fields to Product interface)
  - `components/pre-order-countdown.tsx` (new file - countdown component, badge, info display)
  - `components/product-detail-client.tsx` (modified - pre-order UI integration)
  - `components/product-grid.tsx` (modified - pre-order badges and countdown on cards)
  - `lib/i18n.ts` (modified - added preOrder translations for EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **Prisma fields**: isPreOrder (Boolean), preOrderReleaseDate (DateTime?), preOrderDepositPercent (Int?), preOrderMaxQuantity (Int?)
  - **PreOrderCountdown**: Countdown timer to release date with days/hours/minutes/seconds
  - **PreOrderBadge**: Compact badge showing "Pre-order" with optional deposit percentage
  - **PreOrderInfo**: Detailed info card with release date, countdown, deposit %, max quantity
  - **Product detail integration**: Pre-order badge next to category, info card after price, blue "Pre-order Now" button
  - **Product grid integration**: Pre-order badge, countdown, blue ring highlight, "Pre-order" stock badge
  - **Cart tracking**: isPreOrder and preOrderReleaseDate stored with cart items
  - **Quantity limits**: maxQuantity respects preOrderMaxQuantity limit
  - **Bilingual support**: Full EN/KH translations for all pre-order UI
- **Learnings for future iterations**:
  - Pre-order products can be added to cart regardless of stock (they're for upcoming products)
  - Used blue color theme (bg-blue-600) to distinguish pre-orders from flash sales (orange)
  - PreOrderInfo component shows deposit amount calculated from price when depositPercent is set
  - Product interface fields are optional (?) to maintain backwards compatibility
  - Ring highlight (ring-2 ring-blue-500/50) provides visual distinction on product cards
---

## 2026-01-13 - US-088
- **Implemented**: Back-in-stock notifications for out-of-stock products
- **Files changed**:
  - `prisma/schema.prisma` (modified - added StockNotification model)
  - `app/api/stock-notifications/route.ts` (new file - GET/POST/DELETE endpoints)
  - `components/stock-notify-button.tsx` (new file - notify me button component)
  - `components/product-detail-client.tsx` (modified - integrated StockNotifyButton)
  - `lib/notification-service.ts` (modified - added back-in-stock notification functions)
  - `app/api/inventory/route.ts` (modified - trigger notifications on restock)
  - `components/pages/stock-notifications-page.tsx` (new file - admin dashboard)
  - `components/sidebar.tsx` (modified - added stock-notifications nav item)
  - `components/admin-dashboard.tsx` (modified - added StockNotificationsPage case)
  - `lib/i18n.ts` (modified - added stockNotifications translations for EN/KH)
  - `scripts/ralph/prd.json` (updated passes: true)
- **Features**:
  - **StockNotification model**: productId, email, notified (Boolean), notifiedAt (DateTime?), customerId (optional)
  - **Unique constraint**: One notification per product-email combination
  - **API endpoints**: GET (admin view + subscription check), POST (subscribe), DELETE (unsubscribe)
  - **StockNotifyButton**: Email input form on out-of-stock products with validation
  - **Auto-notification**: checkAndSendBackInStockNotifications() called when inventory quantity goes from 0 to > 0
  - **Email content**: Product name, shop URL, unsubscribe note
  - **Admin dashboard**: Summary stats (pending/notified/total), filterable table, pagination
  - **Re-subscription**: Users can re-subscribe after being notified if product goes out of stock again
  - **Bilingual support**: Full EN/KH translations for button, form, admin UI
- **Learnings for future iterations**:
  - Use productId_email composite unique constraint for efficient duplicate checking
  - Fire-and-forget pattern for notification sending to avoid blocking inventory updates
  - localStorage used for client-side subscription state persistence
  - Only show notify button when: !inStock && !variantSelectionRequired && !product.isPreOrder
  - Inventory route needs to capture previous quantity before update to detect restocking (0 -> positive)
  - sendBackInStockNotifications() marks each notification as sent after successful email
  - Admin page uses useState + useCallback for data fetching (no React Query needed for simple admin views)
---
